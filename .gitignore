# build
node_modules/
dist/
dist-ssr/
*.local
*.tsbuildinfo
__pycache__/
*.pyc

# env
.env
.env.*
!.env.example

# test
test-results/
playwright-report/
blob-report/
coverage/
# QA-01: Vitest and Playwright JSON for tools/test-report.mjs
reports/

# captured data and test clips (CLS-01, TEST-00): not committed
data/
*.y4m

# models: downloaded by `npm run models:fetch` per public/models/models.json
public/models/*.task
public/models/wasm/
public/models/ort/
*.onnx
!public/models/*.onnx
# CLS-02: the stub model is generated by tools/make-stub-classifier.mjs (models:fetch); the real model is a training artifact: not committed
public/models/classifier-stub.onnx
public/models/classifier.onnx

# editor, OS
# REL-01: Claude Code session config of the development machine (launch.json), not committed
.claude/
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
*.log

# SPIKE-00: sample images, sample model and ORT wasm used only by the spikes (`npm run spikes:fetch`), not committed
public/spike-assets/

# the backend archived under archive/backend/ (D-019): its build and database stay uncommitted
dist-server/
.env
# Branch archive/backend: same ignore list as main so main's build output, models and assets do not show as changes when
# this branch is checked out (GitHub Desktop once deleted them on "discard"); main's files, untracked here, are ignored too.
/src/
/tests/
/tools/
/docs/
/public/
/.github/
/.vscode/
/*.json
/*.ts
/*.js
/*.md
/*.html
/.editorconfig
/.prettierrc
/.prettierignore
!/archive/
