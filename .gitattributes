# REL-01 (D-050): LF trong repo và trong working copy trên mọi hệ (Prettier endOfLine lf, .editorconfig), để
# `prettier --check .` và diff giống nhau giữa Windows (core.autocrlf) và CI Linux.
* text=auto eol=lf
*.png binary
*.jpg binary
*.y4m binary
*.task binary
*.wasm binary
*.onnx binary
*.zip binary
