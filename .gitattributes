# REL-01 (D-050): LF in the repo and in the working copy on every OS (Prettier endOfLine lf, .editorconfig), so that
# `prettier --check .` and diffs behave the same on Windows (core.autocrlf) and on the Linux CI runner.
* text=auto eol=lf
*.png binary
*.jpg binary
*.y4m binary
*.task binary
*.wasm binary
*.onnx binary
*.zip binary
