# Test report: mask, late tasks and close timing (QA-01)

This document records how the section 7 test suite in [WORK-BREAKDOWN.md](WORK-BREAKDOWN.md) is measured and its results, focused on the three questions of work package QA-01: (1) does the buffer handed to the face worker ever contain camera pixels outside the reveal region (hard gate, section 7.2); (2) how are task results that arrive late or belong to an old configuration handled; (3) exactly when does the reveal region close. Every test case is an automated test with numeric assertions; no case concludes by visual inspection. The "Run results" section (section 7) is generated by `npm run test:report` from the Vitest and Playwright JSON, together with the measurements each case records.

## 1. How to read

- A "test case" is one test (Vitest or Playwright); the case name is its title in the code. The "Section 7" column is the table in WORK-BREAKDOWN that the test file covers.
- "Measurement" is what a test records with `note()` (annotation `đo`) and `expectGateClean()` (annotation `gate cứng`) in `tests/e2e/helpers.ts`. The test still asserts these values itself with `expect`; the recorded values are only for cross-checking and to make the report reproducible.
- A "skipped" case is one that needs uncommitted local assets (`public/spike-assets/face.png`, `hands.jpg`): it runs on the development machine and skips itself in CI. The run in section 7 has both assets.
- Per-case time depends on the machine and the level of parallelism; CI runs 2 workers, so it is slower.
- The QA-02 bench (`tests/bench`, `npm run test:bench`) runs with its own Playwright configuration and is not part of this report; its measurements (multiple browsers, locked parameters) are in [benchmark.md](benchmark.md) sections 5 and 6.

## 2. Hard gate (section 7.2)

Merge-blocking condition: in every test case, the buffer sent to the face worker (and later the classifier worker) contains no camera pixels outside the mask's `cameraRect` at send time; with a quadrilateral mask (ROI-02, D-038) the cells inside the bounding box that are not open must be gray padding. Measured at the `onRestrictedFrame` probe (an `ImageData` copy of the letterboxed image right before `transferToImageBitmap`, section 5.9), not on screen. Two independent measurement methods:

1. **Marker colors** (`tests/e2e/restricted.spec.ts`): a synthetic scene with a magenta background (255, 0, 255) outside the window and a blue area (0, 0, 255) covering exactly `cameraRect`. Every valid pixel in the buffer has r = g (blue, gray padding and blends between those two colors); a pixel with r ≠ g is a trace of magenta. The count of r ≠ g pixels must be 0 for the smallest window at the top-left and bottom-right corners, a medium window, a large window (scale < 1), with mirror off, and for a skewed quadrilateral with holes (fake hands). Control at each position: shrinking the blue area by 1 px on each side must make the 1 px magenta border inside `cameraRect` appear in the buffer (bad > 0), proving that the measurement can see a 1 px deviation.
2. **Reference image** (`installGateAudit` in `tests/e2e/helpers.ts`, installed in every case that uses the synthetic source): for each buffer passing the probe, the test rebuilds in the page a reference image from the scene definition (`window.__scenario.scene()`: background, person area, static image) only within the buffer's `roiCam`, crops 1:1, paints the holes of the current mask gray, then letterboxes with the same formula as `src/core/letterbox.ts`, and compares it pixel by pixel with the buffer. A difference of more than 2 levels at any pixel is a failure (pixel outside the ROI, misaligned crop or unpainted hole). This method also covers scenes with a real face image and the fake-hand window; buffers captured while the scene is moving or the image has not loaded are skipped and counted separately.

The result of method 2 is on the "Hard gate measured with reference images" line in section 7; the count of pixels outside the ROI from method 1 is in the "Measurement" column of `restricted.spec.ts`.

Besides the two measurement methods, the following invariants are checked by script and lint on every run: `drawImage(` only in the compositor, `restrictedFrame.ts` and the synthetic source, always with a source rect (I2, I4); `restrictedFrame.ts` does not touch the DOM and crops from `source.drawable` (I1, I3); `src/face/` and `src/classify/` import only `src/core/` (I1); `createImageBitmap(` only in `handClient.ts` (`npm run check:invariants`, `npm run lint:boundaries`).

## 3. Late tasks (section 7.12 and table 7.1)

| Situation | Test case | Measurement method |
|---|---|---|
| Result arriving more than 250 ms late while the region is still open | `faceGate.spec.ts` "delayWorker(500) rồi đóng…" (local) | `delayWorker(500)`: `faceGate.rejected.stale` increases, the old face expires, state `searching` |
| Close while a task is running | same case as above | `delayWorker(500)` then `coverAll`: face empty immediately and after 900 ms, white canvas, 0 overlay pixels; on reopen the face returns with a new epoch |
| Grid or mirror change while a task is running | `face.spec.ts` "đổi lưới hoặc mirror khi tác vụ đang chạy…" | 3 s delay, an rAF poll in the page catches the moment a task has just been sent, then changes the configuration: until `FaceClient.stats.discarded` increases by 1, `faceGate.accepted` and the count of results rejected for epoch or `rejected-task` stay unchanged (read in the same pass); afterwards only new-epoch tasks are accepted (`faceGate.lastAccepted.epoch` equals the current epoch) |
| Window moved while a task is running | `faceGate.spec.ts` "kéo cửa sổ khi tác vụ đang chạy…" (local) | `faceMaxAge(4000)` widens the maximum age (debug scenario), 1 s delay so the task is still running when the window moves 3 cells: the first result the gate accepts after the move belongs to the old ROI (`lastAccepted.roiCam`, age > 250 ms) and the face stays in place (center offset ≤ 12 px from before the move), landmarks only inside the new stageRect; the next result from the new ROI also places the face at that same spot. The snapshot after the move is read in the page on rAF because the face of a result is kept for only 1 s (4 × maximum age) and a poll loop from Node may read late when the whole suite runs in parallel (fixed in UX-02 after one failure under load). Likewise, the "worker mặt sẵn sàng…" case of `face.spec.ts` waits for `faceGate.accepted > 0` instead of only waiting for the client to have a result, because the first few results may be rejected as too old when the worker starts slowly under load (fixed in CLS-01). The "nhãn gắn vào mặt" case of `classify.spec.ts` (CLS-02) also reads the face and the label in the same rAF snapshot in the page |
| Result with old epoch, rejected taskId, too old, mask gone | `faceValidate.test.ts` | unit: each rejection reason, exactly 250 ms is still accepted |
| `rejectAll` keeps busy until the result arrives, then discards it | `faceClient.test.ts` | unit with a fake worker |

## 4. Close timing (sections 7.7, 7.14, 7.16, 7.17)

| Event | Test case | Assertion |
|---|---|---|
| One hand missing (only one hand's points left) | `integration.spec.ts` "tay giả lập mở cửa sổ…"; `hands.spec.ts` one hand leaves the frame (local) | closes with `few-points` in the same frame; face empty; `accepting` off; no more buffers sent for 700 ms; the message names the missing slot |
| Crossed hands (uncertain frame) | `solver.spec.ts` "hai tay chéo nhau…" | closes with `ambiguous-hands`, four slots invalid, epoch unchanged; once uncrossed it reopens on the same cells with a new epoch |
| Four fingertips too close | `solver.spec.ts` "too-small…" | closes with `too-small` although four slots are valid; N min and point age apply immediately, epoch unchanged |
| Point too old | same case as above | a 300 ms stale point → `stale-point`; raising the age to 500 → open; resetting → closed |
| Tab hidden | `integration.spec.ts` "đổi nguồn cửa sổ…" | closes with `tab-hidden` inside the event itself (before rAF), epoch + 1, `accepting` off, white canvas; on becoming visible again it reopens (mouse and hands) |
| Camera stopped, watchdog sees no frames | `integration.spec.ts` "camera thật… dừng" | `no-camera`, white canvas, `accepting` off; on restart it reopens |
| Configuration change (grid, mirror, window source) | `roi.spec.ts`, `mask.spec.ts`, `integration.spec.ts` | `config-changed` then reopens with a new epoch; pixels inside the window match the new layout, outside the window white |
| User (Esc, leaving the hand source) | `roi.spec.ts`, `solver.spec.ts` | `user`, white canvas, no slots left |
| Reveal region state machine, camera and tab gates | `revealState.test.ts`, `closeGate.test.ts` | unit: epoch rules per close reason; `cameraGate`, `visibilityGate` |

## 5. Cases not yet automated

- Running all of section 7 with real hands and a webcam on the development machine (INT-01 step 4) and the 10-second right-hand label check (D-010): needs a webcam, not in the automated suite; recorded in WORK-BREAKDOWN section 7.16. The real-hand cases in the automated suite use the static image `hands.jpg` with the real worker.
- The y4m clip for the fake camera (TEST-00): needs ffmpeg; without it Chromium uses its default fake source (the camera cases still run and pass).
- Person and mannequin metrics (section 7.3, CLS-02): need a model trained on a real dataset; `tools/train/eval.py --doc docs/classifier-report.md`. The automated suite currently checks the pipeline with a stub model (`classify.spec.ts`, section 7.23). 15-minute soak: `npm run test:soak`, results in `docs/benchmark.md` (PERF-01).
- Lighthouse for the landing screen (UX-02, section 7.21): run manually with `npx lighthouse http://localhost:5173/#/ --only-categories=accessibility,best-practices` against the dev server; the 2026-09-18 run with Lighthouse 13.4.1, Chrome 153: accessibility 100, best-practices 96 (deduction for MediaPipe's INFO line via console.error). No added dependency, not in CI.
- Real dataset capture (CLS-01, section 7.22): needs participants who signed the consent form and a mannequin; the automated suite checks dataset mode with the synthetic source (samples are the exact `cameraRect` crop, never the raw frame) and the Python tools with a temporary dataset.
- A first-time user with a real webcam (UX-01, section 7.20): someone with no prior explanation opens the window following only the on-screen guidance; the automated suite checks each message with the fake camera and fake hands, which cannot replace this trial.

## 6. Reproducing

```bash
npm run test:report
```

The command above runs the unit tests with the JSON reporter (`reports/unit.json`), the e2e tests (`reports/e2e.json`, configured in `playwright.config.ts`), then rewrites section 7 with `tools/test-report.mjs`. To regenerate from existing JSON only: `npm run test:report:write`. CI (`.github/workflows/ci.yml`) runs the same check chain on Linux, regenerates section 7 and stores `reports/`, `test-results/` and this document as artifacts; the local cases are skipped there.

## 7. Run results

<!-- report:begin -->
Generated by `npm run test:report` (`tools/test-report.mjs`) from the run at 2026-09-19T22:39:43.751Z on the development machine. Do not edit this section by hand.

### Environment

| Item | Value |
|---|---|
| Operating system | Windows_NT 10.0.26200 (x64) |
| CPU, RAM | 12th Gen Intel(R) Core(TM) i5-12500H, 16 threads, 16 GB |
| Node.js | v24.11.0 |
| Vite / Vitest / Playwright | 8.3.0 / 5.0.1 / 1.63.0 |
| E2E browser | Chrome Headless Shell 153.0.8010.12 (Playwright build 1243), Chromium fake camera (`--use-fake-device-for-media-stream`), 6 parallel workers |
| Model | hand_landmarker float16/1 (sha256 fbc2a30080c3…); face_landmarker float16/1 (sha256 64184e229b26…); @mediapipe/tasks-vision 1.0.1 |
| Local assets | face.png: yes; hands.jpg: yes; camera.y4m: no |

### Unit (Vitest)

47 file, 268 test: 268 pass, 0 fail, 0 skipped; total test time 1.5 s.

| File | Section 7 | Tests | Pass | Fail | Skipped | ms |
|---|---|---|---|---|---|---|
| basePath.test.ts | 7.27 | 10 | 10 | 0 | 0 | 17 |
| buildMask.test.ts | 7.7, 7.17 | 5 | 5 | 0 | 0 | 19 |
| cameraState.test.ts | 7.5 | 10 | 10 | 0 | 0 | 12 |
| cells.test.ts | 7.17, 7.26 | 8 | 8 | 0 | 0 | 17 |
| classifierClient.test.ts | 7.23 | 5 | 5 | 0 | 0 | 12 |
| closeGate.test.ts | 7.16 | 2 | 2 | 0 | 0 | 3 |
| compositor.test.ts | 7.8, 7.12, 7.13, 7.14, 7.17 | 13 | 13 | 0 | 0 | 18 |
| config.test.ts | 7.6, 7.24 | 4 | 4 | 0 | 0 | 5 |
| coords.test.ts | 7.6 | 15 | 15 | 0 | 0 | 18 |
| envText.test.ts | 7.24 | 4 | 4 | 0 | 0 | 8 |
| epoch.test.ts | 7.7 | 2 | 2 | 0 | 0 | 2 |
| faceBoundary.test.ts | 7.11 | 3 | 3 | 0 | 0 | 10 |
| faceClient.test.ts | 7.11, 7.12 | 10 | 10 | 0 | 0 | 18 |
| faceMapping.test.ts | 7.12 | 2 | 2 | 0 | 0 | 4 |
| faceValidate.test.ts | 7.12, 7.17, 7.23 | 7 | 7 | 0 | 0 | 12 |
| fakeHands.test.ts | 7.15 | 3 | 3 | 0 | 0 | 10 |
| fingertips.test.ts | 7.14, 7.26 | 7 | 7 | 0 | 0 | 15 |
| grid.test.ts | 7.6 | 6 | 6 | 0 | 0 | 7 |
| guidance.test.ts | 7.20 | 10 | 10 | 0 | 0 | 10 |
| handBoundary.test.ts | 7.13 | 3 | 3 | 0 | 0 | 27 |
| handClient.test.ts | 7.13 | 5 | 5 | 0 | 0 | 70 |
| handDelegate.test.ts |  | 3 | 3 | 0 | 0 | 4 |
| handLandmarker.test.ts | 7.13 | 5 | 5 | 0 | 0 | 8 |
| handPipeline.test.ts | 7.13 | 3 | 3 | 0 | 0 | 69 |
| handTracker.test.ts | 7.13 | 9 | 9 | 0 | 0 | 17 |
| handWindowSource.test.ts | 7.15, 7.17, 7.26 | 9 | 9 | 0 | 0 | 21 |
| hullSolver.test.ts | 7.17, 7.26 | 8 | 8 | 0 | 0 | 15 |
| landingScene.test.ts | 7.28 | 3 | 3 | 0 | 0 | 80 |
| latency.test.ts | 7.19 | 3 | 3 | 0 | 0 | 6 |
| letterbox.test.ts | 7.10 | 6 | 6 | 0 | 0 | 6 |
| localLog.test.ts | 7.25 | 11 | 11 | 0 | 0 | 19 |
| networkGuard.test.ts | 7.27 | 5 | 5 | 0 | 0 | 41 |
| oneEuro.test.ts | 7.15 | 5 | 5 | 0 | 0 | 10 |
| recorder.test.ts | 7.22 | 6 | 6 | 0 | 0 | 110 |
| rect.test.ts | 7.12 | 5 | 5 | 0 | 0 | 6 |
| registerSw.test.ts | 7.27 | 5 | 5 | 0 | 0 | 7 |
| restrictedFrame.test.ts | 7.10, 7.17, 7.22 | 9 | 9 | 0 | 0 | 23 |
| revealState.test.ts | 7.7 | 6 | 6 | 0 | 0 | 10 |
| sensitivity.test.ts | 7.15 | 2 | 2 | 0 | 0 | 4 |
| session.test.ts | 7.21 | 2 | 2 | 0 | 0 | 2 |
| stats.test.ts | 7.19 | 4 | 4 | 0 | 0 | 10 |
| store.test.ts | 7.6, 7.14, 7.16 | 4 | 4 | 0 | 0 | 8 |
| stubModel.test.ts | 7.23 | 1 | 1 | 0 | 0 | 673 |
| subjectRule.test.ts | 7.23 | 8 | 8 | 0 | 0 | 9 |
| sw.test.ts | 7.27 | 6 | 6 | 0 | 0 | 60 |
| uiState.test.ts | 7.20, 7.28 | 3 | 3 | 0 | 0 | 8 |
| zip.test.ts | 7.22 | 3 | 3 | 0 | 0 | 9 |

### E2E (Playwright)

19 spec files, 65 cases: 65 pass, 0 pass after retry, 0 fail, 0 skipped (missing local assets); total case time 1002.0 s (run in parallel).

Hard gate measured against reference images (`installGateAudit`, the second method of section 7.2): 1494 buffers in 32 cases, 1494 matching, 0 deviating, largest difference 0/255.

#### camera.spec.ts (section 7.5)

| Case | Result | Time | Measurements |
|---|---|---|---|
| bật camera giả: getUserMedia một lần sau khi bấm, video ẩn, canvas vẫn trắng, frameId liên tục | pass | 7.9 s |  |
| từ chối quyền: báo lỗi, canvas trắng, bấm lại thì thử lại | pass | 4.5 s |  |
| track kết thúc (rút camera): về ended, canvas trắng, bật lại được | pass | 4.9 s |  |
| watchdog báo khi video ngừng cấp frame; đổi camera tăng epoch và frameId không trùng | pass | 7.7 s |  |

#### classify.spec.ts (section 7.23, 7.2)

| Case | Result | Time | Measurements |
|---|---|---|---|
| worker phân loại: sẵn sàng (webgpu hoặc wasm), đóng thì không gửi; mở trên nửa xanh lá → person, dời sang magenta → mannequin, nhịp ≤ 5 Hz; cửa sổ nhỏ hơn 96 px không gửi; đóng xóa nhãn | pass | 65.4 s | EP wasm, init 31586 ms, warm-up 293 ms, infer p50 1.2 / p95 4.2 ms, nhịp 250 ms; 7 kết quả trong 2 s; gửi 24, loại 0, gate nhận 23, loại {"epoch":0,"rejected-task":0,"stale":1,"no-mask":0}<br>227 buffer so tham chiếu: 227 khớp, 0 lệch, maxDiff 0 (bỏ qua 9 cảnh động, 0 chờ ảnh) |
| nhãn gắn vào mặt đã validate (face.png cục bộ): cùng epoch, subjectType theo quy tắc, xóa khi đóng | pass | 57.8 s | mặt full (nhìn thấy 100 %), subjectType unknown, độ tin cậy 0.533, probs 0.467/0.533<br>157 buffer so tham chiếu: 157 khớp, 0 lệch, maxDiff 0 (bỏ qua 0 cảnh động, 0 chờ ảnh) |

#### dataset.spec.ts (section 7.22, 7.2)

| Case | Result | Time | Measurements |
|---|---|---|---|
| dataset mode: cần đồng ý người tham gia; chỉ báo khi thu; mẫu là crop đúng cameraRect với pixel khớp cảnh; 2 Hz; đóng vùng hay dừng thì không thu; zip đúng cấu trúc; không gọi mạng | pass | 53.1 s | 8 mẫu 160×160 px (cameraRect (560, 200)), khoảng cách ts nhỏ nhất 527 ms, zip 17555 byte với 17 mục, ranh giới xanh lá/magenta tại x = 80<br>201 buffer so tham chiếu: 201 khớp, 0 lệch, maxDiff 0 (bỏ qua 0 cảnh động, 0 chờ ảnh) |
| cửa sổ chạm mép camera ghi position edge (mirror: cột 0 của bảng là mép phải camera); nhịp thu chỉnh được | pass | 5.3 s | cửa sổ (0, 0) trên lưới 64 × 36, mirror true: cameraRect (1120, 0) 160×160, edges top, right; 4 mẫu ở 10 Hz, khoảng cách ts nhỏ nhất 105 ms<br>7 buffer so tham chiếu: 7 khớp, 0 lệch, maxDiff 0 (bỏ qua 0 cảnh động, 0 chờ ảnh) |

#### face.spec.ts (section 7.11, 7.12, 7.1, 7.2)

| Case | Result | Time | Measurements |
|---|---|---|---|
| worker mặt sẵn sàng; đóng 5 s không gửi tác vụ; mở vào vùng nền thì gửi và kết quả 0 mặt; đóng thì ngừng nhận | pass | 50.9 s | worker GPU, init 15061 ms; đóng 5 s: 0 tác vụ; mở vào nền: 37 tác vụ, 37 kết quả 0 mặt, p50 infer 51.8 ms; đóng: không gửi thêm<br>37 buffer so tham chiếu: 37 khớp, 0 lệch, maxDiff 0 (bỏ qua 0 cảnh động, 0 chờ ảnh) |
| đổi lưới hoặc mirror khi tác vụ đang chạy: kết quả epoch cũ bị bỏ trước gate, gate không nhận thêm; tác vụ epoch mới mới được nhận | pass | 53.9 s | grid: epoch 3 → 5; tác vụ cũ bị bỏ trước gate (discarded 0 → 1), gate không nhận thêm tới lúc đó (4); sau đó nhận 12, chỉ epoch mới; loại vì epoch: 0<br>mirror: epoch 5 → 7; tác vụ cũ bị bỏ trước gate (discarded 1 → 2), gate không nhận thêm tới lúc đó (14); sau đó nhận 17, chỉ epoch mới; loại vì epoch: 0<br>130 buffer so tham chiếu: 130 khớp, 0 lệch, maxDiff 0 (bỏ qua 0 cảnh động, 0 chờ ảnh) |

#### faceGate.spec.ts (section 7.12, 7.1, 7.2)

| Case | Result | Time | Measurements |
|---|---|---|---|
| mặt trọn trong cửa sổ: full, landmark và overlay chỉ trong stageRect, bbox camera nằm trong ảnh | pass | 52.0 s | full: 478 landmark trong stageRect, bbox 89×103 px; overlay trong 2209 px, ngoài 0 px; gate nhận 2, loại cũ 9<br>120 buffer so tham chiếu: 120 khớp, 0 lệch, maxDiff 0 (bỏ qua 0 cảnh động, 0 chờ ảnh) |
| cửa sổ cắt mặt: partial, không landmark ngoài vùng, overlay không ra ngoài, gợi ý mở rộng | pass | 33.7 s | partial (mặt hở 65 %): 404 landmark, tất cả trong stageRect; overlay trong 1486 px, ngoài 0 px<br>57 buffer so tham chiếu: 57 khớp, 0 lệch, maxDiff 0 (bỏ qua 0 cảnh động, 0 chờ ảnh) |
| delayWorker(500) rồi đóng: mặt biến mất ngay và không hiện lại; mở lại thì có mặt; kết quả quá tuổi bị loại | pass | 34.3 s | delayWorker(500) rồi đóng: mặt rỗng ngay và sau 900 ms, overlay 0 px; mở lại epoch 2 → 5; kết quả muộn > 250 ms bị loại: stale 5 → 6<br>22 buffer so tham chiếu: 22 khớp, 0 lệch, maxDiff 0 (bỏ qua 0 cảnh động, 0 chờ ảnh) |
| kéo cửa sổ khi tác vụ đang chạy: kết quả cũ ánh xạ theo ROI của tác vụ (mặt không dời theo cửa sổ), landmark chỉ trong stageRect mới | pass | 51.4 s | dời 3 ô khi tác vụ đang chạy: kết quả ROI cũ (tuổi 1247 ms, 478 landmark) đặt mặt lệch (0.0, 0.0) px so với trước khi dời; kết quả ROI mới lệch (0.1, 0.4) px; landmark đều trong stageRect mới<br>93 buffer so tham chiếu: 93 khớp, 0 lệch, maxDiff 0 (bỏ qua 0 cảnh động, 0 chờ ảnh) |

#### grid.spec.ts (section 7.6)

| Case | Result | Time | Measurements |
|---|---|---|---|
| lưới mặc định 64 × 36: canvas theo DPR, c và bảng đúng công thức, vạch lưới xám; tắt vạch thì toàn trắng | pass | 3.9 s |  |
| preset, custom có giới hạn và mirror: layout tính lại, epoch tăng | pass | 5.1 s |  |
| đổi kích thước cửa sổ: canvas và layout tính lại, epoch tăng | pass | 6.7 s |  |

#### hands.spec.ts (section 7.13, 7.14, 7.15, 7.17, 7.26)

| Case | Result | Time | Measurements |
|---|---|---|---|
| worker tay chỉ khởi tạo khi chọn nguồn "Tay"; nền không có tay thì HandFrame rỗng; rời nguồn tay thì dừng và xóa | pass | 27.7 s |  |
| nhãn theo S3 (D-010), overlay hai màu, cửa sổ đóng; id giữ khi đứng yên và khi ảnh di chuyển; đảo trái/phải thì nhãn đảo và id mới | pass | 40.3 s | 3 buffer so tham chiếu: 3 khớp, 0 lệch, maxDiff 0 (bỏ qua 1 cảnh động, 0 chờ ảnh) |
| mười đầu ngón (ROI-03): đủ điểm khi hai tay trong khung, đúng tay và đúng ngón; cửa sổ bao lồi theo tay thật; bỏ ngón út thì epoch tăng và còn 8 điểm; tay phải rời khung thì đóng few-points, điểm tay trái giữ | pass | 51.5 s | 18 buffer so tham chiếu: 18 khớp, 0 lệch, maxDiff 0 (bỏ qua 22 cảnh động, 0 chờ ảnh) |

#### integration.spec.ts (section 7.16, 7.2)

| Case | Result | Time | Measurements |
|---|---|---|---|
| đổi nguồn cửa sổ là đổi cấu hình (epoch++); tab ẩn đóng ngay với tab-hidden và epoch++, accepting tắt, canvas trắng; hiện lại thì mở lại (chuột và tay) | pass | 14.9 s | epoch: mở 3 → tay 4 → chuột 6 → tab ẩn 7 → hiện 8; tay giả lập: ẩn/hiện 11 → 13, cùng hộp (26, 14) 12×8<br>22 buffer so tham chiếu: 22 khớp, 0 lệch, maxDiff 0 (bỏ qua 0 cảnh động, 0 chờ ảnh) |
| camera thật (giả của Chromium) dừng hay ngừng cấp frame thì vùng đóng no-camera và canvas trắng; chạy lại thì mở lại | pass | 14.0 s |  |
| tay giả lập mở cửa sổ thì worker mặt nhận buffer; bỏ một tay thì vùng đóng cùng frame, mặt xóa, accepting tắt, không gửi thêm | pass | 25.8 s | bỏ tay phải: đóng few-points, buffer gửi 4 rồi đứng yên 700 ms, accepting tắt<br>121 buffer so tham chiếu: 121 khớp, 0 lệch, maxDiff 0 (bỏ qua 0 cảnh động, 0 chờ ảnh) |
| tay giả lập bao quanh mặt: face-candidate với overlay trong cửa sổ; bỏ một tay thì đóng và mặt biến mất ngay | pass | 24.5 s | 10 buffer so tham chiếu: 10 khớp, 0 lệch, maxDiff 0 (bỏ qua 0 cảnh động, 0 chờ ảnh) |

#### landing.spec.ts (section 7.4, 7.21)

| Case | Result | Time | Measurements |
|---|---|---|---|
| trang chào không gọi getUserMedia; sau đồng ý mới vào #/app với canvas trắng; không gọi mạng | pass | 7.1 s |  |
| chưa đồng ý mà mở #/app thì về trang chào | pass | 3.3 s |  |
| đồng ý phiên bản cũ không còn hiệu lực | pass | 6.3 s |  |
| thu hồi đồng ý thì về trang chào và không vào lại được #/app | pass | 9.7 s |  |

#### log.spec.ts (section 7.25)

| Case | Result | Time | Measurements |
|---|---|---|---|
| mặc định tắt; bật thì ghi consent; phiên camera giả, cửa sổ chuột, đổi lưới, dừng camera → đúng loại sự kiện, chỉ metadata, CSV tải về, bảng lọc, giữ qua tải lại, xóa; không gọi mạng | pass | 18.7 s | 6 sự kiện trong phiên: consent, camera-start, reveal-open, reveal-close, config-change, camera-stop; CSV 6 dòng, 709 ký tự; payload lớn nhất 110 byte; yêu cầu ngoài tài nguyên tĩnh: 0 |
| giới hạn: bản ghi quá 30 ngày bị xóa, quá 10 000 bản ghi xóa cũ trước; xóa về 0 | pass | 16.0 s | ghi thẳng 10 008 bản ghi (3 quá 30 ngày) → còn 10000 sau khi dọn; xóa → 0 |

#### mask.spec.ts (section 7.8, 7.1)

| Case | Result | Time | Measurements |
|---|---|---|---|
| camera chạy, mở cửa sổ: pixel camera chỉ trong stageRect và khớp ánh xạ cameraRect → stageRect; tắt mirror thì lật | pass | 9.1 s | mirror bật: maxDiff 0/255 trên 25 điểm, 25 điểm có video; mirror tắt: maxDiff 0; lưới 32 × 18: maxDiff 0, epoch 6 → 8; 4 điểm ngoài trắng |
| dời cửa sổ thì vị trí cũ trắng ngay; đóng thì toàn bộ trắng | pass | 10.7 s |  |

#### present.spec.ts (section 7.28, 7.2)

| Case | Result | Time | Measurements |
|---|---|---|---|
| thanh trên một dòng ở 1280 px kể cả khi câu trạng thái dài và nhật ký bật; bậc nút: chỉ Bật camera là primary, Xóa nhật ký là danger; pill trạng thái có chấm theo pha | pass | 7.3 s | thanh trên cao 48 px (có nhật ký: 48 px), 0 nút đen, primary = rgb(25, 103, 210) |
| cột cài đặt 320 px bên phải: thu gọn trả lại chiều rộng; chọn nguồn Tay không đổi cỡ canvas; ngăn kéo debug dưới canvas có giới hạn chiều cao; thanh trượt và chip | pass | 7.6 s | cột cài đặt 320 px; canvas 960×672 (chuột) = 960×672 (tay); mở hết 960×522, ô 14 px, ngăn kéo 150 px; thu gọn cột 1280×522 |
| chế độ trình diễn: panel thành lớp nổi không đổi cỡ canvas (epoch giữ nguyên), tự ẩn sau 2,5 s rồi hiện lại khi di chuột, giữ qua tải lại; ?mode=present mở sẵn với cột cài đặt đóng | pass | 11.4 s | trình diễn: canvas 1280×720 = .stage; mở cột cài đặt và debug giữ 1280×720, epoch 3; lớp nổi ẩn sau 2,5 s<br>31 buffer so tham chiếu: 31 khớp, 0 lệch, maxDiff 0 (bỏ qua 0 cảnh động, 0 chờ ảnh) |
| màn hình bắt đầu kiosk (?mode=present): minh họa canvas phủ cả màn, thẻ đồng ý nổi, không ảnh, không tài nguyên ngoài; đồng ý thì vào #/app?mode=present ở chế độ trình diễn | pass | 2.8 s | kiosk: nền canvas 1280×720, không cuộn; tương phản h1 16.1:1 · .lead 10.47:1 · label.consent span 10.47:1 · .steps li 6.05:1 |

#### restricted.spec.ts (section 7.2, 7.10, 7.17)

| Case | Result | Time | Measurements |
|---|---|---|---|
| gate cứng: buffer 256 × 256 không có pixel ngoài cameraRect ở nhiều vị trí và cỡ cửa sổ, kể cả sát mép và nhỏ nhất; đối chứng 1 px | pass | 7.7 s | (0, 0) n=4: cameraRect 80×80, pixel ngoài ROI 0/65536, xanh 65536, xám 0; đối chứng viền 1 px lọt 5020 pixel<br>(60, 32) n=4: cameraRect 80×80, pixel ngoài ROI 0/65536, xanh 65536, xám 0; đối chứng viền 1 px lọt 5020 pixel<br>(10, 5) n=12: cameraRect 240×240, pixel ngoài ROI 0/65536, xanh 65536, xám 0; đối chứng viền 1 px lọt 2032 pixel<br>(20, 2) n=20: cameraRect 400×400, pixel ngoài ROI 0/65536, xanh 65536, xám 0; đối chứng viền 1 px lọt 1020 pixel<br>mirror tắt (2, 2) n=8: pixel ngoài ROI 0; 42 buffer qua probe, 0 bitmap tới worker phân loại<br>42 buffer so tham chiếu: 42 khớp, 0 lệch, maxDiff 0 (bỏ qua 0 cảnh động, 0 chờ ảnh) |
| chỉ đổi nội dung ngoài cửa sổ: hash buffer không đổi; đổi nội dung trong cửa sổ: hash đổi | pass | 6.1 s | hash fe959dc5 giữ nguyên khi chỉ đổi nền (frame 25 → 37); đổi màu trong cửa sổ → 990f9dc5<br>14 buffer so tham chiếu: 14 khớp, 0 lệch, maxDiff 0 (bỏ qua 0 cảnh động, 0 chờ ảnh) |
| đổi n: crop đúng cỡ cameraRect, letterbox đúng công thức, góc crop ánh xạ ngược về đúng góc cameraRect | pass | 6.5 s | n=4: crop 80×80, letterbox scale 3.2000, dx 0, dy 0<br>n=9: crop 180×180, letterbox scale 1.4222, dx 0, dy 0<br>n=16: crop 320×320, letterbox scale 0.8000, dx 0, dy 0<br>13 buffer so tham chiếu: 13 khớp, 0 lệch, maxDiff 0 (bỏ qua 0 cảnh động, 0 chờ ảnh) |
| đóng thì không tạo tác vụ; cửa sổ quá nhỏ thì too-small, không tiêu taskId; mở rộng thì taskId từ 1 | pass | 5.6 s | đóng 15 frame: tác vụ 3 → 3, too-small 3 → 3<br>3 buffer so tham chiếu: 3 khớp, 0 lệch, maxDiff 0 (bỏ qua 0 cảnh động, 0 chờ ảnh) |
| gate cứng với tứ giác lệch (tay giả lập): lỗ trong hộp bao là xám, không có pixel ngoài ô mở; đối chứng 1 px | pass | 7.8 s | hộp 15×10 ô, 98 ô mở, 52 lỗ: pixel ngoài ô mở 0, xanh 28338 (tỉ lệ mở 0.653 → 28545), xám 36788; đối chứng 479<br>27 buffer so tham chiếu: 27 khớp, 0 lệch, maxDiff 0 (bỏ qua 0 cảnh động, 0 chờ ảnh) |

#### roi.spec.ts (section 7.7)

| Case | Result | Time | Measurements |
|---|---|---|---|
| Space mở cửa sổ n = 8 giữa bảng với viền xanh trong stageRect; Esc đóng; epoch tăng khi mở và khi đóng | pass | 4.7 s |  |
| bấm mở tại con trỏ, kéo dời, kéo ra mép thì kẹp với viền đỏ, lăn chuột đổi n | pass | 5.3 s |  |
| đổi lưới khi đang mở: đóng với config-changed rồi mở lại giữ chỗ trên màn hình với epoch mới | pass | 4.0 s |  |
| nguồn cửa sổ mặc định là chuột; chọn tay được (HAND-01) và đóng cửa sổ đang mở | pass | 3.9 s |  |

#### solver.spec.ts (section 7.15, 7.17, 7.26, 7.1, 7.2)

| Case | Result | Time | Measurements |
|---|---|---|---|
| tay giả lập mở đúng tập ô; đứng yên và rung ±3 px không đổi ô; dời và phóng theo tay không đổi epoch; rời nguồn tay thì đóng | pass | 7.4 s | chữ nhật 12,3 × 8,2 ô → hộp (26, 14) 12×8, 96 ô, 0 lỗ; đứng yên và rung ±3 px: cùng hộp, epoch 5 giữ nguyên; dời 4 ô → hộp (29, 14) 13×8; phóng → (28, 13) 16×10; cùng epoch<br>18 buffer so tham chiếu: 18 khớp, 0 lệch, maxDiff 0 (bỏ qua 0 cảnh động, 0 chờ ảnh) |
| đa giác lệch: ô bị cạnh cắt qua mở, ô ngoài đa giác trong hộp bao là lỗ (không vẽ video, không gửi worker); đa giác nét đứt | pass | 4.3 s | hình thang lệch → hộp (26, 14) 12×9: 79 ô mở, 29 lỗ; (37,14) (37,17) (32,15) tắt, (37,22) (26,14) (26,21) (32,16) mở; lỗ trên canvas trắng, ô mở có video<br>4 buffer so tham chiếu: 4 khớp, 0 lệch, maxDiff 0 (bỏ qua 0 cảnh động, 0 chờ ảnh) |
| too-small khi các đầu ngón quá gần; tuổi điểm và độ nhạy (UC-09) áp dụng ngay | pass | 6.2 s | cạnh 2,5 ô → too-small; điểm cũ 300 ms → stale-point, tuổi 500 → mở, đặt lại → đóng; N min 9 → too-small, 3 → mở; epoch 6 → 7 (chỉ khi mở lại); hysteresis 0 → hộp (25, 13) 14×10<br>11 buffer so tham chiếu: 11 khớp, 0 lệch, maxDiff 0 (bỏ qua 0 cảnh động, 0 chờ ảnh) |
| hai tay chéo nhau (frame uncertain): đóng ambiguous-hands với mọi đầu ngón không hợp lệ, canvas trắng, epoch giữ; hết chéo thì mở lại cùng ô | pass | 5.3 s | uncertain → đóng ambiguous-hands, epoch 5 giữ; hết chéo → mở lại hộp (26, 14), epoch 6<br>2 buffer so tham chiếu: 2 khớp, 0 lệch, maxDiff 0 (bỏ qua 0 cảnh động, 0 chờ ảnh) |
| năm đầu ngón mỗi tay (mặc định): vùng mở là bao lồi của mười điểm, tập ô khớp tính lại trong Node; chọn lại hai ngón thì về hình chữ nhật với epoch mới | pass | 6.6 s | mười đầu ngón → bao lồi 7 đỉnh, hộp (22, 13) 16×9, 130 ô khớp tính lại trong Node, 14 lỗ; hai ngón → hộp (26, 14) 12×8, 96 ô, epoch 4 → 10<br>6 buffer so tham chiếu: 6 khớp, 0 lệch, maxDiff 0 (bỏ qua 0 cảnh động, 0 chờ ảnh) |

#### start.spec.ts (section 7.21, 7.28)

| Case | Result | Time | Measurements |
|---|---|---|---|
| một màn ở 1280 × 720 và 1920 × 1080 (không cuộn), không tài nguyên ngoài, minh họa canvas vẽ tại chỗ trang trí, không ảnh | pass | 4.7 s | 1280 × 720: scrollHeight 720 ≤ clientHeight 720, không cuộn<br>1920 × 1080: scrollHeight 1080 ≤ clientHeight 1080, không cuộn<br>minh họa canvas 517 × 291 CSS px, 517 × 291 px |
| điện thoại 390 × 844: một cột, không cuộn ngang, nút Bắt đầu vẫn tới được | pass | 3.1 s | 390 × 844: scrollWidth 390 ≤ clientWidth 390, 1 cột, scrollHeight 973 |
| bàn phím: Tab tới hộp đồng ý (nút chưa bật bị bỏ qua), Space tích, Tab tới Bắt đầu, Enter vào #/app; nhãn hộp đồng ý đọc được | pass | 2.2 s | thứ tự Tab: body → hộp đồng ý → Bắt đầu (sau khi tích) → Enter vào #/app |
| khối "đã đồng ý trước đó" có đường dẫn vào thẳng và đi trước nút trong thứ tự Tab sau nút Bắt đầu | pass | 2.6 s |  |
| tương phản AA: chữ, cam kết, nhãn đồng ý, chữ mờ, nút Bắt đầu, liên kết đều ≥ 4,5:1 (tiêu đề lớn ≥ 3:1) | pass | 2.1 s | tiêu đề 16.1:1 (40.96px) · giới thiệu 10.47:1 (17px) · cam kết 16.1:1 (15px) · nhãn đồng ý 16.1:1 (15px) · chữ mờ 6.05:1 (14px) · liên kết 5.37:1 (14px) · nút Bắt đầu 5.37:1 (16px) · ba bước 6.05:1 (13px) · chú thích minh họa 6.05:1 (13px) |

#### stats.spec.ts (section 7.19, 7.2)

| Case | Result | Time | Measurements |
|---|---|---|---|
| overlay hiệu năng: fps output > 0, mặt ≤ 12 Hz với tác vụ chờ ≤ 1, p95 ≥ p50, dòng stats-stat đúng dạng; đóng vùng thì mặt 0 Hz | pass | 21.0 s | mở vùng: output 58.6 fps, mặt 10.5 Hz (p50 43.5 / p95 54.5 ms, nhịp 83 ms, rớt 1, chờ 1), vẽ p50 0.20 / p95 0.40 ms, tick p95 5.90 ms<br>đóng vùng: mặt 0.0 Hz, chờ 0, output 60.0 fps<br>21 buffer so tham chiếu: 21 khớp, 0 lệch, maxDiff 0 (bỏ qua 0 cảnh động, 0 chờ ảnh) |
| tay giả lập theo quỹ đạo: cửa sổ đổi ô liên tục mà epoch giữ nguyên, fps output ổn định, tác vụ chờ ≤ 1 | pass | 7.4 s | quỹ đạo 3 ô / 4 s: 10 vị trí hộp trong 4,5 s, epoch 4 giữ nguyên; output 57.0 fps, mặt 0.0 Hz, vẽ p95 0.70 ms, tick p95 6.90 ms<br>32 buffer so tham chiếu: 32 khớp, 0 lệch, maxDiff 0 (bỏ qua 0 cảnh động, 0 chờ ảnh) |

#### synthetic.spec.ts (section 7.9, 7.2)

| Case | Result | Time | Measurements |
|---|---|---|---|
| nguồn tổng hợp: không gọi camera thật; cửa sổ bên phải xanh lá, bên trái magenta; tắt mirror thì đảo; đóng thì trắng | pass | 6.3 s | 23 buffer so tham chiếu: 23 khớp, 0 lệch, maxDiff 0 (bỏ qua 0 cảnh động, 0 chờ ảnh) |
| probe onOutputFrame nhận ImageData toàn canvas theo nhịp; delayWorker ghi vào probes; cảnh đổi được | pass | 5.9 s | 8 buffer so tham chiếu: 8 khớp, 0 lệch, maxDiff 0 (bỏ qua 0 cảnh động, 0 chờ ảnh) |
| ?debug=1 không có source: camera thật vẫn là nguồn, probe bật, kịch bản có sẵn | pass | 5.3 s |  |
| không có ?debug=1: không có probe, không có kịch bản | pass | 4.7 s |  |

#### ux.spec.ts (section 7.20, 7.28, 7.2)

| Case | Result | Time | Measurements |
|---|---|---|---|
| hướng dẫn ba bước với camera giả và cửa sổ chuột: bật camera → mở cửa sổ → tìm mặt; Esc và dừng camera đưa về bước trước, canvas trắng | pass | 14.9 s | chuỗi thông điệp: Bật camera để bắt đầu → Mở cửa sổ bằng chuột → Đang tìm khuôn mặt trong cửa sổ → Mở cửa sổ bằng chuột → Bật camera để bắt đầu |
| hướng dẫn theo tay giả lập trên nguồn tổng hợp: mỗi lý do đóng có thông điệp riêng; tab ẩn về bước camera; mở thì sang bước khuôn mặt | pass | 16.7 s | chuỗi thông điệp: Đang nạp bộ nhận diện tay… → Còn thiếu đầu ngón → Đang tìm khuôn mặt trong cửa sổ → Hai tay chéo nhau → Mất dấu đầu ngón → Các đầu ngón quá gần nhau → Đầu ngón ra ngoài bảng → Tab đang ẩn<br>7 buffer so tham chiếu: 7 khớp, 0 lệch, maxDiff 0 (bỏ qua 0 cảnh động, 0 chờ ảnh) |
| panel cài đặt thu gọn được (canvas nhận lại chỗ theo chiều rộng, UX-03), panel debug tách riêng và mặc định đóng khi không có ?debug=1; trạng thái giữ qua tải lại trong tab | pass | 8.2 s | canvas 960×672 px khi mở cột cài đặt, 1280×672 px khi thu gọn (epoch 1 → 2), 1280×522 px khi mở ngăn kéo debug |
| toàn màn hình: nút và phím F; thanh và panel thành lớp phủ (canvas không đổi cỡ khi lớp phủ ẩn), tự ẩn sau khi không tương tác, chuột hiện lại | pass | 8.2 s | toàn màn hình 1280×720 CSS px, canvas bằng đúng .stage; epoch 3 giữ nguyên khi lớp phủ ẩn và hiện<br>7 buffer so tham chiếu: 7 khớp, 0 lệch, maxDiff 0 (bỏ qua 0 cảnh động, 0 chờ ảnh) |

### E2E on the production build (Playwright, `npm run test:deploy`, REL-01)

`dist/` via `vite preview` (playwright.deploy.config.ts), projects chromium, chrome: 12 cases: 12 pass, 0 fail, 0 skipped; total time 16.2 s (sequential).

#### tests/deploy/sw.spec.ts (section 7.27)

| Case | Result | Time | Measurements |
|---|---|---|---|
| [chromium] khóa cache trong dist/sw.js: MODELS_KEY là sha256 rút gọn của models.json, BUILD_ID theo dist/assets | pass | 0.0 s | khóa model 5c1bcc341da7, build 395222ab7d36, base / |
| [chromium] trang đầu (chặn đăng ký): đặt đồng ý và tạo hai cache wct-* cũ để ca sau kiểm activate xóa chúng | pass | 0.2 s |  |
| [chromium] lần mở 1 vào #/app: worker kích hoạt và điều khiển trang, xóa cache cũ, cache đủ model, wasm, loader ORT theo EP và asset của trang | pass | 3.4 s | EP wasm → loader jsep; cache model 7 file, cache app 7 file; lần 1 tải 12 phản hồi models/ + assets/, 40.1 MB theo Content-Length |
| [chromium] lần mở 2: trang được điều khiển từ đầu, mọi phản hồi models/ và assets/ đến từ service worker và worker không xin mạng cho chúng (cache hit) | pass | 2.6 s | EP wasm; 12/12 phản hồi models/ và assets/ từ service worker; worker chỉ ra mạng 1 lần (trang): / |
| [chromium] lần mở 3 offline: trang, asset, model và loader ORT từ cache; worker mặt và phân loại sẵn sàng, cửa sổ mở | pass | 2.4 s | offline: EP wasm, worker mặt và phân loại sẵn sàng, vùng mở |
| [chromium] không yêu cầu nào rời origin của trang trong cả ba lần mở (I9, đo ở context gồm cả service worker) | pass | 0.0 s | 64 yêu cầu, 27 tới models/, 0 khác origin |
| [chrome] khóa cache trong dist/sw.js: MODELS_KEY là sha256 rút gọn của models.json, BUILD_ID theo dist/assets | pass | 0.0 s | khóa model 5c1bcc341da7, build 395222ab7d36, base / |
| [chrome] trang đầu (chặn đăng ký): đặt đồng ý và tạo hai cache wct-* cũ để ca sau kiểm activate xóa chúng | pass | 0.3 s |  |
| [chrome] lần mở 1 vào #/app: worker kích hoạt và điều khiển trang, xóa cache cũ, cache đủ model, wasm, loader ORT theo EP và asset của trang | pass | 4.2 s | EP webgpu → loader asyncify; cache model 7 file, cache app 7 file; lần 1 tải 12 phản hồi models/ + assets/, 38.5 MB theo Content-Length |
| [chrome] lần mở 2: trang được điều khiển từ đầu, mọi phản hồi models/ và assets/ đến từ service worker và worker không xin mạng cho chúng (cache hit) | pass | 1.6 s | EP webgpu; 12/12 phản hồi models/ và assets/ từ service worker; worker chỉ ra mạng 1 lần (trang): / |
| [chrome] lần mở 3 offline: trang, asset, model và loader ORT từ cache; worker mặt và phân loại sẵn sàng, cửa sổ mở | pass | 1.4 s | offline: EP webgpu, worker mặt và phân loại sẵn sàng, vùng mở |
| [chrome] không yêu cầu nào rời origin của trang trong cả ba lần mở (I9, đo ở context gồm cả service worker) | pass | 0.0 s | 67 yêu cầu, 26 tới models/, 0 khác origin |

<!-- report:end -->
