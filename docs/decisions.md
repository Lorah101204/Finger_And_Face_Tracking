# Design decisions

Each row is one locked decision. To change a decision, add a new row and state which row it replaces; do not edit the old row. The proposed defaults are in section 3 of [WORK-BREAKDOWN.md](./WORK-BREAKDOWN.md); measurements are in [spikes.md](./spikes.md).

| Date | ID | Decision | Rationale | Related |
|---|---|---|---|---|
| 2026-09-17 | D-001 | Use all defaults from section 3 of WORK-BREAKDOWN.md as the starting point; the values live in `src/core/config.ts` | Allows starting right away; tuning happens in QA-02 | Section 3, appendix 9.1 |
| 2026-09-17 | D-002 | `.task` models and wasm are not committed; they are downloaded with `npm run models:fetch` per `public/models/models.json`, with sha256 pinned on the first download | Lightweight repo; source, version and hash are still recorded; runs without depending on a CDN | SETUP-00 |
| 2026-09-17 | D-003 | `src/face/**` and `src/classify/**` may import all of `src/core/**` (types and pure math), not only types and config | `faceMapping` needs `cameraToStage` in `core/coords`; `core/` has no path to the raw frame | I1, section 4.2 |
| 2026-09-17 | D-004 | Workers have their own tsconfig (`tsconfig.worker.json`, lib WebWorker), separate from the app (lib DOM) | Avoids DOM/WebWorker type conflicts; workers do not see the DOM | SETUP-00 |
| 2026-09-17 | D-005 | HTTPS in dev is enabled with `VITE_HTTPS=1` via `@vitejs/plugin-basic-ssl`; the default is http on localhost | localhost is already a secure context for getUserMedia | CAM-01 |
| 2026-09-17 | D-006 | e2e runs Chromium with a fake camera; `--use-file-for-fake-video-capture` is added only when `tests/e2e/fixtures/camera.y4m` exists | Tests can run right away, before TEST-00 exists | TEST-00, QA-01 |
| 2026-09-17 | D-007 | The project scaffold is written by hand following the Vite react-ts template (`create-vite` is not run because the directory already has `docs/` and the CLI prompts interactively) | Reproducible, independent of the CLI's prompts | SETUP-00 |
| 2026-09-17 | D-008 | FACE-01: `face.worker.ts` is a module worker (Vite bundle), GPU delegate, calls `FilesetResolver.forVisionTasks(base, true)`; `base` is `/node_modules/@mediapipe/tasks-vision/wasm` in dev and `/models/wasm` in the build (constant in `core/config.ts`, checked by a build test in QA-01); warm-up with one gray image right after init; if GPU init fails, recreate with CPU | S1: GPU 14 ms versus CPU 39 ms per image, independent of crop size; a module worker only runs with the ES module loader; Vite dev refuses `import()` of files in `public/`. Classic worker + dynamic `import()` is a tested fallback | S1, FACE-01 |
| 2026-09-17 | D-009 | HAND-01: Hand Landmarker runs in `hand.worker.ts` (same module worker pattern as D-008), receives the raw frame via `createImageBitmap` + transfer; the delegate is configurable, default CPU for hands on this machine, re-measure with a real webcam and lock in QA-02 | S2: transferring a 720p frame costs 0.1 ms; detect takes 50 to 70 ms off the main thread; with two hands CPU at 49 ms is faster than GPU at 62 ms in the worker | S2, HAND-01, PERF-01 |
| 2026-09-17 | D-010 | HAND-01: `normalizeHandedness` is the identity function when the frame fed to the model is not mirrored (the design always feeds the raw frame; mirroring happens only at display); the flag `hands.handednessSwap` swaps the labels if a real webcam gives reversed results; the first step of HAND-01 is a 10 second check with a real right hand | S3: tasks-vision 1.0.1 returns labels matching the anatomical hand on unmirrored images, contrary to the old note in the MediaPipe docs | S3, HAND-01 |
| 2026-09-17 | D-011 | CAM-01: the video element is in the DOM but hidden with `opacity: 0; position: fixed; left: -10000px` (`display: none` is not used even though Chromium still fires rVFC); `requestVideoFrameCallback` is the `FrameStamp` source, with rAF fallback; `visibilitychange` to hidden puts the region into Closed(tab-hidden); a watchdog is added: no FrameStamp for longer than `camera.noFrameWatchdogMs` (500 ms) gives Closed(no-camera); `MediaStreamTrackProcessor` is not used in the PoC | S4 phase 1: rVFC fires fully with every way of hiding the video element; phase 2 cannot be measured automatically, and the host may stop media without changing visibilityState in time (hidden pane of the embedded browser) | S4, CAM-01 |
| 2026-09-17 | D-012 | MASK-02: keep the 1:1 `drawImage` path into an OffscreenCanvas sized exactly to `cameraRect`, letterbox, then `transferToImageBitmap`, one bitmap per worker; the `onRestrictedFrame` probe (needs `getImageData`) runs only when debug is on and is capped at 2 Hz; dataset mode takes its copy via a worker or at a sparse rate | S5: crop, letterbox and transfer cost 0.1 ms on a real GPU; `getImageData` costs 9 to 14 ms (p95 up to 29 ms) | S5, MASK-02, TEST-00, CLS-01 |
| 2026-09-17 | D-013 | CLS-02: ONNX Runtime Web with execution providers `['webgpu', 'wasm']` (webgpu first, wasm as fallback); the model is exported with a fixed input of 128 or 160 (no reliance on dynamic axes); the ORT loader follows the environment as in D-008; consider COOP/COEP to enable multi-threaded wasm only when needed | S6: single-threaded wasm 59 ms, webgpu 19 ms for MobileNetV2 224 on an RTX 3050; the model zoo model is fixed at 224, so 128 fails | S6, CLS-02 |
| 2026-09-17 | D-014 | Spike code lives in `tools/spikes/` and runs on the dev server; the headless runner `tools/spikes/run-spike.mjs` only allows measurements unrelated to background tabs; spike assets in `public/spike-assets/` are not committed | Playwright disables background throttling; the tool environment cannot spawn `chrome.exe`; the embedded browser pane may be hidden | SPIKE-00 |
| 2026-09-17 | D-015 | Add a web platform layer: Node.js + Fastify + TypeScript backend in `server/`, SQLite database via `node:sqlite` for the PoC (Postgres for a multi-user deployment, same `Repo` layer), frontend with routes `/`, `/app`, `/admin`; one Node process serves both `dist/` and `/api` behind an HTTPS reverse proxy | New requirement: explicit backend, frontend and database; `node:sqlite` ships with Node 24, no native module needed | A11, section 4.7, API-00, DEP-01 |
| 2026-09-17 | D-016 | Visitors use an anonymous session (signed `sid` cookie, httpOnly, SameSite=Lax), display name optional; they must consent on the `/` page (versioned form) before entering `/app`; `getUserMedia` is called only after `assertCameraAllowed()` and from a click action | Requirement for a simple interface before allowing camera use; invariant I10 | WEB-00, CAM-01 |
| 2026-09-17 | D-017 | Metadata-only log (event type list in `shared/events.ts`, payload ≤ 2 KB, ≤ 50 events per send); the server rejects everything else; retained 90 days; the IP may have its last octet masked; never receives frames, crops or landmarks | Invariant I9; requirement for the admin to view visitor logs without breaking the original plan's privacy rules | LOG-01, ADM-01, SEC-01 |
| 2026-09-17 | D-018 | Admin logs in with an account in the `users` table (admin role, scrypt), `adm` cookie for 12 hours, created initially from `ADMIN_USERNAME`/`ADMIN_PASSWORD`; CSRF protection via SameSite + `Sec-Fetch-Site`/`Origin` checks; rate limits of 120 requests/minute and 5 logins/minute | No dependency on external services; sufficient for the PoC and an internal pilot | ADM-01, SEC-01 |
| 2026-09-17 | D-019 | Replaces D-015, D-016, D-017, D-018: return to a static, client-only app as in the original plan. No backend, database, visitor log, admin page or telemetry; hosting is static HTTPS. Keep the landing page `/` with the consent button (versioned form, stored in `localStorage` under the key `wct.consent`) and the stage `/app`; `getUserMedia` is still called only after `assertCameraAllowed()` (I10). Backend, admin and telemetry code and their tests move to `archive/backend/`, outside build, lint and tests; delete after committing if not needed | Requirement re-confirmed: web client only, no user statistics needed; a backend makes the client depend on a process it does not need and brings in operations, security and privacy concerns (IP, user-agent) that the original plan deliberately avoids | A11, A12, section 4.7, WEB-00 |
| 2026-09-17 | D-020 | Routing uses `HashRouter` (`/#/app`) instead of `BrowserRouter`; `cameraGateReason()` checks the path via the hash (`currentRoute()` in `gate.ts`) | `dist/` runs on any static host, including GitHub Pages, without an SPA fallback; there is no server to configure rewrites on (D-019) | WEB-00, REL-01, section 4.7 |
| 2026-09-17 | D-021 | Consent scope is configurable at `DEFAULTS.consent.scope`: `device` (localStorage, persists across loads, default) or `tab` (sessionStorage, expires when the tab closes, for kiosk and presentation); the "Thu hồi đồng ý" (Revoke consent) button is kept in both | A shared presentation machine must not inherit the previous person's consent | WEB-00, UX-02 |
| 2026-09-17 | D-022 | No visitor log. If an operations log is needed, build the optional package LOG-02: metadata events written to the browser's own IndexedDB, an in-place viewing table and CSV export; nothing leaves the browser (I9). If cross-machine visit statistics are needed, use the static host provider's statistics, no code | Replaces the role of the dropped LOG-01 and ADM-01 without a backend | LOG-02, I9 |
| 2026-09-17 | D-023 | The `/` page is the site's landing screen, with visual design in package UX-02: name, short introduction, privacy commitment, consent and the "Bắt đầu" (Start) button on one screen; still no camera call, no network call; the form structure, `CONSENT_VERSION` and consent gate of WEB-00 stay unchanged, and WEB-00's e2e must pass after the redesign | Requirement: the initial consent page becomes an aesthetically designed landing screen | UX-02, WEB-00, I10 |
| 2026-09-17 | D-024 | REL-01 consists of: CI runs `models:fetch`, `lint`, `lint:boundaries`, `check:invariants`, `test:unit`, `test:e2e`, the Mermaid check, `build`, then pushes `dist/` to a static HTTPS host; a service worker caching models and wasm is optional (kiosk, fast subsequent loads, offline); `archive/backend/` is committed to the separate branch `archive/backend` in the first commit and is not on `main` | Static, client-only packaging; keeps `main` clean | REL-01 |
| 2026-09-17 | D-025 | CAM-01: `CameraSource` receives a `gate` (the app passes `assertCameraAllowed`) and calls it synchronously right before every `getUserMedia`, including when switching cameras or retrying; `getUserMedia` appears only in `src/camera/cameraSource.ts` (checked by `tools/check-invariants.mjs`). `frameId` increases monotonically over the lifetime of `CameraSource` and does not reset when switching cameras. Pure state machine in `cameraState.ts` (idle, requesting, active, ended, error); `stop()` returns to idle; watchdog and hidden tab are the flags `stalled` and `hidden` in the snapshot, not states; `cameraCloseReason()` derives `no-camera` or `tab-hidden` for the loop. `epoch.bump()` on every entry into requesting and every exit from active | I10 forces every path to `getUserMedia` through the gate; a non-repeating frameId is a CAM-01 criterion; the pure state machine is split out for unit tests in Node | CAM-01, D-011, I10 |
| 2026-09-17 | D-026 | GRID-01: `computeLayout(stage, grid, cam)` takes the camera size and `Layout` carries `cam`; while the camera is not active, the store uses the default camera size (`DEFAULTS.camera`, 1280 × 720) so the grid shows before the camera is started, then recomputes with epoch++ when the camera becomes active with the real size. `camVisibleRect` keeps real numbers; `windowToCameraRect(win, layout, mirror)` rounds each edge (`Math.round`) then clips, so adjacent windows share edges in both spaces; the compositor and restrictedFrame use exactly this rect. Grid lines `#e6e6e6`, 1 device px at every cell boundary including the board edge (the right and bottom edge lines lie inside the board); e2e treats white and this color as "no camera pixel". Toggling grid lines does not change the epoch; changes to cols, rows, mirror, stage size or camera size do epoch++. One epoch counter is shared by the camera and the stage store (`src/loop/store.ts`). Grid settings are not yet persisted across loads (UX-01 to consider) | The camera size is a required input of `scale` and `camVisibleRect` that the signature in section 4.1 lacked; the grid must show immediately on entering `#/app` (step 1 of the experience); one epoch for every source of change (A7) | GRID-01, section 4.1, MASK-01 |
| 2026-09-17 | D-027 | ROI-00: `WindowSource.current(now)` returns `{ window, limited }` already clamped inside the board, or `{ window: null, reason }`. Mouse: clicking on the board opens a window at the pointer (default n `reveal.mouseInitialN` = 8), drag to move, wheel to change n around the center, Esc closes, Space reopens at the previous center or the board center; CSS coordinates convert to stage px by the ratio `canvas.width / rect.width`; when the layout changes, the window is re-placed from its stage px center so it keeps its place on screen. `buildMask` (step 1 of MASK-01) is done in ROI-00 because the window border and RevealState need the mask. The `frameLoop` runs on rAF (the frame of section 4.4: WindowSource, buildMask once, RevealState, compositor); INT-01 connects the pipelines. State machine in `core/revealState.ts`: closed → open increments the epoch; open → closed increments for tab-hidden, no-camera, user; config-changed does not increment here because the store already incremented exactly once on the config change; hand-related reasons do not increment. Closing because the camera stops or the tab is hidden is wired in INT-01 (ROI-00 must be able to verify the geometry without the camera on). Window border 2 px `#1a73e8`, red `#d93025` when limited, drawn inside stageRect. Changing the window source does not increment the epoch; the loop closes the window itself | ROI-00 criterion: verify all board and window geometry without hands (and without a camera); one epoch for every source of change, never incremented twice for the same cause | ROI-00, MASK-01, sections 4.6, 5.7 |
| 2026-09-17 | D-028 | MASK-01: `render(ctx, layout, { showLines, mirror, drawable, mask })` in `mask/compositor.ts` is the only place that drawImages onto the output: white background → grid lines → (only when there is a mask and a `drawable`) `drawImage(cameraRect → stageRect)` inside `ctx.clip(stageRect)`, mirroring via `translate(s.x + s.w, s.y)` + `scale(-1, 1)` only within stageRect → border. `drawable` is `FrameSource.drawable` (the hidden video or the synthetic canvas), null while the camera is not active: an open window shows only its border. Every frame is fully redrawn, nothing accumulates. `check:invariants` checks that `drawImage(` appears only in `compositor.ts` and `restrictedFrame.ts` and always with 9 arguments (with a source rect). The MASK-01 e2e uses Chromium's fake camera because the synthetic source only arrives with TEST-00: stop the video, then compare the output against a reference image drawn in the page from the same cameraRect and mirror, difference ≤ 8 levels per channel; TEST-00 adds absolute color checks. Closing the region when the camera stops or the tab is hidden is still left to INT-01 | Invariants I2 (one mask) and I4 (white before video): no path draws a full-frame; checked by script so it holds when later packages are added | MASK-01, TEST-00, INT-01 |
| 2026-09-17 | D-029 | TEST-00: the synthetic source and probes are enabled by a query in the hash (`#/app?debug=1` enables the probes `window.__wct.probes` and the scenario `window.__scenario`; adding `&source=synthetic` replaces the camera with `SyntheticCameraSource`, no `getUserMedia` call, the "Bật camera" (Start camera) button is disabled). Scenarios drive the window through `MouseWindowSource` (`setWindow`, `moveWindow`, `resizeWindow`, `close`) instead of a separate window source; `delayWorker(ms)` writes `probes.workerDelayMs` for FACE-02. Default scene: left half of the camera green `#00ff00`, right half magenta `#ff00ff`, no face; e2e checks absolute colors inside the window. The output probe reads back at a rate (default 250 ms) and only when there is a listener; the restricted probe is capped at 2 Hz and is called by MASK-02. y4m clips are generated with `tools/make_test_clips.py` (needs ffmpeg, default 640 × 480, 20 fps, 4 s ≈ 37 MB, not committed); the current machine has no ffmpeg, so only `--dry-run` is checked | No product code branches added for tests; every debug hook sits behind `?debug=1` and nothing leaves the browser (I9); a 720p 10 s y4m is too large (400 MB) | TEST-00, MASK-02, FACE-02 |
| 2026-09-17 | D-030 | MASK-02: `createRestrictedFrameBuilder({ probes })` in `mask/restrictedFrame.ts` is the only place that creates a `RestrictedFrame`; `build(source, mask, stamp, epoch, taskId, copies)` takes a `FrameSource` (not a loose drawable, so the source can only be the hidden video or the synthetic canvas, never the output canvas; `check:invariants` forbids `document`, `querySelector`, `getElementById`, `HTMLCanvasElement` in this file and requires `source.drawable`) and returns `{ kind: 'ok', frames }` (one bitmap per worker: redraw the letterbox and transfer again), `too-small` (short side of `cameraRect` < `face.minRoiPx`, no canvas touched, no taskId consumed) or `no-frame`. Two reused canvases: the crop sized exactly to `cameraRect` (width/height changed only when the size changes), the letterbox at `face.inputSize` filled with gray `face.padGray`. Letterbox: `scale = size / max(w, h)`, `dx, dy = floor((size − side·scale) / 2)`, destination size kept as real numbers; the math is in `core/letterbox.ts` so FACE-02 can map back without importing `mask/`. The loop calls build at step 6 after drawing the output, when the region is open, the source has a new frame (`frameId` differs from last time) and the `face.targetHz` tick is due; there is no worker yet, so the bitmap is closed immediately; `taskId` counts up from 1 only on ok. The probe receives the `ImageData` of the letterboxed image right before transfer, at the probes' rate. The hard gate e2e uses a color palette with a channel invariant (blue inside the window, magenta outside, gray padding: a valid pixel always has r = g) and a control case that shrinks the person region by 1 px to prove the measurement sees a 1 px deviation | Hard gate 7.2 must be measured at the probe with a source that cannot be the output; the letterbox math is separated from the canvas so face/ can use it (I1); no empty task is created when too small (section 5.9) | MASK-02, FACE-02, INT-01, section 5.9 |
| 2026-09-17 | D-031 | FACE-01: protocol in `face/faceProtocol.ts` (imports only `core/`): `init`, `detect` (carries a `RestrictedFrame`, transfers `input`), `ready`, `result`, `error` (a null `taskId` means an init error). `face.worker.ts` is a module worker, `forVisionTasks(base, useModuleLoader)` then create with GPU, on failure recreate with CPU, warm-up with a gray image of `face.inputSize`, `input.close()` in `finally`. `FaceClient` is the only place that creates the face worker, `start()` is called when `StagePage` mounts; `submit()` always takes ownership of the bitmap (transfers it or closes it immediately on drop); `rejectAll()` marks every submitted taskId, busy is held until the worker returns; suggested rate `minIntervalMs = max(1000 / face.targetHz, p50 inferMs)` over the last 20 samples (targetHz stays 12 per D-001, not 15). Loop: turn `accepting` on when the region opens, call `rejectAll` and turn it off on close or when the mask reopens with a new epoch; build a buffer when the worker is idle or a probe wants to read; count `faceDetectSubmitted` on a successful submit and `faceDetectDropped` for every buffer that could not be submitted (not ready, busy). Debug: `window.__wct.face.snapshot()`, the debug bar and a thumbnail of the last submitted buffer drawn with `putImageData` from the `onRestrictedFrame` probe (only with `?debug=1`). E2E runs the real worker in headless Chromium, accepts GPU or CPU, waits for ready up to 60 s; the per-test e2e timeout is raised from 30 to 60 s because every page initializes the face worker in parallel | One worker receives exactly one kind of image-carrying message (I1); no task queueing (section 5.10); the rate never exceeds the real inference speed | FACE-01, FACE-02, INT-01, sections 4.5, 5.10 |
| 2026-09-17 | D-032 | FACE-02: `validateFace(result, task, ctx)` in `face/faceValidate.ts` (imports only `core/` and files in the same directory) returns `rejected` with reason `epoch`, `rejected-task`, `stale` (age measured from the `ts` of the submitted frame, threshold `freshness.faceResultMaxAgeMs`), `no-mask`, or `ok` with `ValidatedFace[]` and the number of dropped faces; the full margin is `face.fullFaceMarginRatio` × the short side of the ROI; landmarks map to stage using the current layout (a layout change changes the epoch and the result is rejected earlier, so this is also the layout of the task's epoch). Rectangle math in `core/rect.ts`, mapping in `face/faceMapping.ts`. Loop: results arrive via `FaceClient.subscribeResults` and are validated on arrival; the face list is kept until the next result (a 0-face result also replaces it), cleared on close or reopen with a new epoch, and expires after 4 × `faceResultMaxAgeMs` without a new result so the overlay does not freeze when the worker stops returning. The overlay is drawn inside `ctx.clip(stageRect)` after the video and before the border: full bbox `#188038` solid, partial `#f9ab00` dashed, landmarks as 2 px dots. States `covered`, `too-small` (short side of `cameraRect` < `face.minRoiPx`, computed from the current mask), `partial-face`, `face-candidate`, `searching`; `FrameOutput` is emitted every frame via `loop.events` (CustomEvent `frame`) and is available in `snapshot().output`; `points` stays empty until HAND-02. The `delayWorker` scenario delays at `FaceClient.resultDelayMs` (busy is held during the delay) instead of inside the worker. The real-face e2e runs locally with `public/spike-assets/face.png` (spike asset, not committed) and skips itself when the file is missing; the e2e that needs no image checks `searching`, `covered` and the `frame` event | Decision tree 5.10 and FACE-02 criteria: covering again makes the face disappear on the very next frame, moving the window does not drag the face along, partial has no landmarks under the white area, results arriving after close are not shown; avoid a frozen overlay when the worker is slow | FACE-02, INT-01, UX-01, sections 4.4, 4.6, 5.10 |
| 2026-09-17 | D-033 | HAND-01: protocol in `hands/handProtocol.ts` (`init`; `detect` carries a `HandRawFrame` = unmirrored full-frame bitmap with `frameId`, `ts`, `epoch`, transferred; `ready`, `result`, `error` with a null `frameId` meaning an init error). `hand.worker.ts` is a module worker in VIDEO mode, `numHands` 2, delegate per `hands.delegate` (CPU, D-009), on failure switch to the other delegate, warm-up with a gray image, timestamps never go backwards, `input.close()` in finally. `HandClient` (`hands/handClient.ts`) is the only place in `src/` that calls `createImageBitmap` (checked by `check:invariants`, I1): one frame in flight at a time, a new frame while busy is dropped (no queueing), can be started again after dispose (React StrictMode remounts). `normalizeHandedness(label, { inputMirrored, swap })` in `hands/handLandmarker.ts` is the identity function for the raw frame (D-010), swapped per `settings.handednessSwap` (new flag in `StageSettings`, checkbox "Đảo trái/phải" (Swap left/right), does not change the epoch, changing it resets the tracker). `HandTracker` (`hands/handTracker.ts`, pure): cost = distance between palm centers (mean of landmarks 0, 5, 9, 13, 17) / camera width + `handednessPenalty` 0.1 when the labels differ (below `matchCostMax` 0.15 so a label flickering for one frame does not split the track; a reversed label for `relabelFrames` = 3 consecutive frames makes the track change handedness while keeping its id; `score` = 1 − score when the label is reversed); greedy matching by increasing cost; `uncertain` when one detection or one track has two candidates differing by less than `ambiguityDelta` 0.03: keep the track as is, do not update `lastSeenTs`, do not create a new track; a track is removed when more than `trackDropMs` 150 ms have passed since it was last seen AND it has been absent for at least `trackDropFrames` = 2 consecutive updates (with a 20 Hz pipeline this equals the plan's 150 ms rule exactly; with a pipeline slower than 150 ms per frame, one missed frame does not remove the track; an uncertain frame counts as absent); a bitmap size change resets; ids increase monotonically and are never reused; `HandFrame` returns a copy. `HandFrame` is kept in `HandPipeline` (`hands/handPipeline.ts`, `latest`; read via `loop.snapshot().hands` and `window.__wct.hands`) instead of `StageStore` so React does not re-render at 20 Hz. The loop feeds the pipeline only when `settings.windowSource` is `hands` (the hand worker initializes lazily on the first feed, so mouse mode and the older e2e pay nothing extra); leaving the hand source resets; there is no `HandWindowSource` yet, so the hand source closes with `missing-slot` until INT-01. The hand debug overlay (`compositor.drawHands`) is drawn after the border on the white background, including outside `stageRect`: bbox, five fingertips, center, labels "Trái #id"/"Phải #id" (Left/Right #id), faded when older than `pointMaxAgeMs`, dashed when uncertain. Landmark indices are in `core/handLandmarks.ts`; `rectCameraToStage` moves to `core/coords.ts`. The 10 second check with a real right hand could not be done on the development machine (the tool environment has no webcam): the operator does it manually and enables "Đảo trái/phải" if the labels are reversed. The real-hands e2e runs locally with `public/spike-assets/hands.jpg` (S3) and skips itself when missing; in headless Chromium the CPU delegate settles at about 100 ms per frame (the first few frames up to one second, GPU/SwiftShader 540 ms), so the test waits for it to settle and measures by result count; the synthetic scene of the hand test cases runs at 8 fps and the cases waiting for workers (face, hands) to be ready wait up to 90 to 120 s because the whole suite running in parallel makes initialization take over a minute, Playwright is limited to 6 workers (16-thread machine), the debug bar is fixed to one line so changing status text does not resize the canvas and bump the epoch mid-test, camera.spec measures steady fps between two reads instead of from the first frame; image motion in `SyntheticCameraSource` is measured from when the image is set so motion can be enabled after the pipeline settles without a jump | HAND-01 criteria (ids unchanged when two hands move normally, crossed hands give uncertain instead of swapping ids), D-009, D-010, section 4.4 step 2, 5.4, 5.8; avoid running the hand worker when not in use | HAND-01, HAND-02, INT-01, PERF-01 |
| 2026-09-17 | D-034 | HAND-02: `hands/slots.ts` is pure, `evaluateSlots(frame, configs, layout, mirror, now)` re-evaluates the four slots on every render frame from the latest `HandFrame` (no separate slot state): the track of the same hand with the highest score, never a different hand; tip landmarks are already camera px → `cameraToStage`; reasons in the priority order of section 5.8 `ambiguous-hands` → `missing-slot` → `out-of-board` → `stale-point` (age = now − the track's `lastSeenTs`, threshold `freshness.pointMaxAgeMs`), plus `low-score` when score < `hands.minTrackScore` = 0.5 (closes the region with `missing-slot`). `settings.slots` in `StageStore` with the preset `DEFAULTS.slots`; changing a slot does `epoch++` without recomputing the layout, and an open window closes with `config-changed` (UC-03); the `SlotControls` UI (hand and finger per slot) shows only when the window source is hands. Overlay `compositor.drawSlots`: 8 px dots colored per slot (`SLOT_COLORS`) with a number, faded (alpha 0.4) when invalid but still holding a point, hidden when there is no point, drawn after the hand overlay. `FrameOutput.points` is filled every frame when the source is hands, empty with the mouse; `LoopSnapshot.slots` for debug and e2e; `statusMessage(output, slots)` while the region is covered with the hand source returns the `slotsGuidance` guidance ("Đưa hai tay vào khung hình: thiếu …" (Bring both hands into the frame: missing …), with variants for stale, out of board, hand unclear, crossed hands). There is no solver yet (ROI-01), so the hand source closes with `slotsCloseReason`, and with `missing-slot` when all four slots are present. In headless CPU, point age at render time is roughly 100 to 150 ms, so e2e accepts `stale-point` interleaved with valid and treats only `missing-slot` as a lost hand | HAND-02 criteria: losing a finger makes the slot invalid within at most 150 ms plus one render frame (evaluated every frame from the last seen time); no slot ever switches to another hand on its own (matching only within the same handedness); section 5.8, UC-03, UC-15 | HAND-02, ROI-01, INT-01, UX-01 |
| 2026-09-18 | D-035 | ROI-01: pure `reveal/oneEuro.ts` (`stepOneEuro(state, x, ts, params)`, the state is an immutable record, the first sample has no lag, dt ≤ 0 is skipped) and pure `reveal/squareSolver.ts` (`solveSquare(points, layout, prev, ts, opts)`): center = mean of the four points, side = min(bboxW, bboxH) stage px, One Euro filtering for cx, cy and side; `sideCells < nMin` → `too-small` (with hysteresis while open: closes only when dropping below `nMin − h`, reopening needs the full `nMin`); n, then col and row, are rounded from the center with hysteresis `h = 0.25` cells (`snapWithHysteresis`: changes only when the rounding boundary is crossed by more than h) relative to the previous **requested** (unclamped) window; when n changes, col and row are re-rounded around the center (the window zooms around its center); `clampWindow` clamps into the board while keeping it square and `limited` = requested window differs from the clamped window, so `limited` stays set as long as the hands keep requesting a window that overflows the board. `HandWindowSource` (`reveal/handWindowSource.ts`) is the `WindowSource` of the hand source: each `current(now)` evaluates the four slots (`hands/slots.ts`, point age per sensitivity), returns the section 5.8 reason with `slots` when one is missing, and when all four are present solves exactly once per new `HandFrame` using the `frame.ts` timestamp (One Euro filters at the hand result rate, not the render rate); the solver state resets on closed → open (section 4.6), when layout, mirror or the slot configuration changes identity, and on `reset()`. `WindowSample` gains `slots`, so the loop no longer evaluates slots itself; close reasons from the hand source go straight into `RevealState` (no epoch change, D-027) | ROI-01 criteria: with hands held still the window does not flicker (unit: ±3 px jitter around a cell boundary does not change the cell; with hysteresis 0 it flickers), n changes smoothly, near the edge it stays square and has `limited`; sections 5.8, 4.6 | ROI-01, INT-01, QA-02 |
| 2026-09-18 | D-036 | ROI-01 step 4 (UC-09): sensitivity lives in `StageStore.settings.sensitivity` (`reveal/sensitivity.ts`: `minCutoff`, `beta`, `hysteresisCells`, `nMin`, `pointMaxAgeMs`, defaults from `core/config.ts`, clamped by `SENSITIVITY_LIMITS`), the `SensitivityControls` bar only when the source is hands, applied on the next frame, no epoch change, button "Đặt lại độ nhạy" (Reset sensitivity); `statusMessage` says "chạm mép bảng (bị kẹp)" (touching the board edge, clamped) when `limited` and "quá gần nhau: tách hai tay ra" (too close together, separate the hands) when `too-small`. Debug (TEST-00): `HandPipeline.setFake(gen)` replaces the worker with fake `HandFrame`s generated on each `feed(source, epoch, now)` (the worker is not started), the `window.__scenario.hands(spec)` scenario with `debug/fakeHands.ts` (21 landmarks, thumb tip at (x, y + spread/2), index tip at (x, y − spread/2), fake jitter that is pseudo-random and repeatable by frameId, `ageMs`, `uncertain`); `window.__wct.handWindow.snapshot()` and the `solver-stat` line for e2e. In headless CPU the real point age is roughly 100 to 200 ms, so the real-hand e2e raises "Tuổi điểm" (point age) to 1000 ms so the window opens stably; the real-hand e2e no longer asserts that the region is closed when enough hands are present | e2e needs to test the solver and the hand window in CI without real hand images or a webcam; UC-09 requires adjustment at runtime | ROI-01, QA-01, QA-02, UX-01 |
| 2026-09-18 | D-037 | INT-01: `loop/closeGate.ts` with `CloseGate { reason(), subscribe() }`: `cameraGate(camera)` follows the camera snapshot's `cameraCloseReason` (hidden tab first, then no-camera when not yet active, while switching camera or on watchdog), `visibilityGate(document)` for the synthetic source (hidden tab only). The loop asks `reason()` every frame before the window source (when the gate is closed the source is not asked and the slots are empty) and also registers via `subscribe` so that `closeNow(reason)` runs inside the event itself: close, clear faces, `rejectAll`, `accepting = false`, paint white, emit `FrameOutput`, because in a hidden tab `requestAnimationFrame` does not run and face results arriving later must be rejected by the new epoch. Stopping or switching the camera bumps the epoch twice (CameraSource on leaving active and `stepReveal` with no-camera): accepted, the epoch only needs to increase. Changing the window source is a configuration change: `StageStore` bumps the epoch (without recomputing the layout), the loop closes with `config-changed` without bumping again (D-027); the mouse window logically stays open, so switching back to mouse or the gate clearing reopens on the next frame (closed → open, epoch++). `statusMessage` says the tab is hidden or the camera has not delivered a frame yet. ClassifierClient not wired yet (CLS-02). Consequence for e2e: the mouse window opens only when the camera delivers frames, so the ROI-00 cases and the `windowAt` scenario with the real camera start the fake camera first (`startFakeCamera`) and no longer check for white pixels inside the window (inside the window is video, mask.spec checks it); when mask.spec pauses the video to compare pixels it compares inside the page right away and then resumes playback, because after 500 ms the watchdog closes the region. Step 4 (running all of section 7 with real hands on the development machine) could not be done because the tooling environment has no webcam: one manual run is needed together with the D-010 check step | Section 4.6 and UC-15: hidden tab, camera stop and configuration change must close the region and reject running tasks; INT-01 step 3 | INT-01, QA-01, UX-01, CLS-02 |
| 2026-09-18 | D-038 | ROI-02 (replaces the square part of D-035 and the point "the four points are the controller" in section 2.1): the hand reveal region is a **quadrilateral whose four vertices are the four fingertips**, the mask is the **cell set** intersecting the quadrilateral, including cells only cut through by an edge (the square locked in the 14/09 plan was a mistake). `RevealShape` = square window (mouse) or `quad` (stage px, vertices sorted by angle around the center into a simple polygon); `RevealMask` = `box` (bounding box in cells) + `cells` (cell bitmap) + `cellCount` + `stageRect`, `cameraRect` (integer rect of the bounding box) + `holesCam` (integer camera rect of each cell inside the box that is not open). Pure `core/cells.ts`: `polygonOverlapsRect` (positive-area intersection: vertex inside the cell, cell corner inside the polygon, edge passing through the cell interior; touching an edge does not count; corners inset by 1e-6), `rasterizePolygon` with per-cell hysteresis (an open cell stays open while it still touches the cell expanded by h·c, a closed cell turns on only when the polygon enters the cell shrunk by h·c; h = `sensitivity.hysteresisCells`), `cellOutlineEdges`. `reveal/quadSolver.ts` replaces `squareSolver`: One Euro per coordinate (8 filters), `too-small` when the short side of the bounding box < nMin cells or the area < nMin²/2 cells² (nearly coincident, nearly collinear), with hysteresis while open; no snapping, no clamping (the points are inside the board so the quadrilateral does not overflow the board, `limited` remains only for mouse). `buildMask(shape, layout, mirror, epoch, { limited, prev, hysteresisCells })` is the only place that rasterizes (I2), `prev` is the previous frame's mask within the same open session. The compositor clips with a path that is the union of the open cells (a single rect when the box is full) and then one `drawImage` of the bounding box; the border is the boundary edges of the cell union inset by 1 px, the quadrilateral is dashed; faces clip with the same path. `restrictedFrame` crops the bounding box and then paints the `holesCam` with gray padding before letterboxing (I1: the buffer holds only open-cell pixels). `validateFace`: full when the bbox (stage) lies entirely within the union of open cells and within the ROI inset by 4 %, partial when it touches an open cell, dropped when it lies only in holes; landmarks in holes are filtered out. `FrameOutput.reveal` carries `shape`, `box`, `cells`. The loop closes with `too-small` if the cell set is empty. Hysteresis consequence: an edge cell overlapped by less than 0.25 cells keeps its previous state (when the hands move, the old edge column leaves after the new column enters), the e2e states this explicitly | User request of 18/09: the reveal region of the four fingers is not necessarily square, it is an arbitrary four-sided shape; white cells cut through by an edge must also show. The cell set preserves I1 to I4 (the mask still decides both display and buffer, no image distortion) with pure geometry testable by unit tests | ROI-01, ROI-02, MASK-01, MASK-02, FACE-02, INT-01, QA-01 |
| 2026-09-18 | D-039 | QA-01: the hard gate is measured in two independent ways in every case that uses the synthetic source: marker colors (`restricted.spec`) and a reference image rebuilt from the scene definition (`installGateAudit` in `tests/e2e/helpers.ts`: 1:1 crop by `roiCam`, gray holes, letterbox with the same formula, per-pixel comparison, a difference above 2 levels is an error, animated scenes and images not yet loaded are skipped); e2e records measurements in annotations (`note`, `expectGateClean`); Playwright adds the JSON reporter `reports/e2e.json`, Vitest `--reporter=json --outputFile=reports/unit.json` (not `test-results/` because Playwright wipes that directory at the start of each run); `tools/test-report.mjs` generates the "Run results" section (environment, CPU, Node, Chromium per Playwright's `browsers.json`, models per `models.json`, table of cases and measurements) between two markers in `docs/test-report-mask.md`, the rest is hand-written; `LoopSnapshot.faceGate.lastAccepted` (task, epoch, ROI, age, face count) and the `faceMaxAge(ms)` scenario (`probes.faceMaxAgeMs`, only with `?debug=1`, relaxes the maximum age of face results) so e2e can check the mapping by the old ROI when the window moves, deterministically even when inference is slow under load; CI on GitHub Actions `.github/workflows/ci.yml` is set up already in QA-01 (Node 24, model cache keyed by the manifest sha256, lint, boundaries, invariants, Prettier, Mermaid, unit, e2e with 2 workers and one retry when `CI`, report generation, build; artefacts `reports/`, `test-results/`, the report, `dist/`), leaving REL-01 only the deployment step; `tools/spikes` is excluded from Prettier because spike code stays as it was when measured | QA-01 requires every case to be measured numerically, run in CI and have a reproducible report; the two hard-gate measurements catch both crop offsets and unpainted holes in every static scene, not just the marker-color scene | QA-01, REL-01 |
| 2026-09-18 | D-040 | PERF-01: `core/latency.ts` (sliding p50/p95 window as a ring buffer, rank sorted[floor(p · (n − 1))] so p50 keeps the FaceClient convention) used for inferMs of FaceClient and HandClient (adds `p95InferMs`) and for the loop's draw time and tick (`LoopSnapshot.timing`, 120-frame window); `debug/stats.ts` is a 250 ms sampler computing output fps, face, hand and classifier Hz over a 2 s window (Hz = Δcount / Δt), drops, pending, epoch, state; the `stats-stat` overlay via `useSyncExternalStore(stats.subscribe)` and `window.__wct.stats` (`debug/statsProbe.ts`, split out so stats.ts does not pull global declarations into unit tests); rate control keeps `max(1000 / targetHz, p50)` (FACE-01); soak `tests/soak/soak.spec.ts` with its own `playwright.soak.config.ts` (`npm run test:soak`, `SOAK_MINUTES` default 15, `SOAK_SAMPLE_S` 30, one worker), heap after GC, nodes and listeners via CDP `Performance.getMetrics` after `HeapProfiler.collectGarbage` (not `measureUserAgentSpecificMemory` because it needs COOP/COEP), real hands on `hands.jpg` when available, otherwise `FakeHandsSpec.orbit` for fake hands moving along an orbit; `tools/benchmark-report.mjs` generates the results section of `docs/benchmark.md` with pass/fail against the section 3 targets; keep Canvas 2D (step 6) while p95 draw time stays under 16.7 ms; headless measurements (SwiftShader, CPU hand delegate) are a lower bound, real browsers are measured in QA-02 | PERF-01 requires reproducible measurements and an automated soak; the sampler is separate from the loop to avoid per-frame allocation, and React re-renders at only 4 Hz | PERF-01, QA-02, CLS-02 |
| 2026-09-18 | D-041 | UX-01: pure `app/guidance.ts`: `buildGuidance` returns the step (camera → window → face), tone, title, one guidance sentence and the close reason for each camera phase (`cameraPhase`, distinguishing a camera switch from the first permission request via an active history kept outside React), each `CloseReason` per window source and each `FrameOutput.status`; priority camera > hidden tab > worker loading or failed > close reason > reveal region state; the `Guide` layer floats over the canvas with `pointer-events: none` and `aria-live`; `statusMessage` (FACE-02) is kept as the debug line. Fullscreen via the Fullscreen API on the root element `.stage` (button and F key): the top bar and both panels become absolute overlays that auto-hide after 2.5 s without interaction, the canvas does not change size so the epoch does not bump when the overlay shows or hides. The settings panel (grid, mirror, source, swap left/right, slots, sensitivity) and the debug panel are separate, collapsed via `hidden` (the debug line stays in the DOM for older e2e), state saved in sessionStorage `wct.ui` per tab, the debug panel open by default only with `?debug=1`. Clicking the canvas removes focus from inputs so Esc, Space and F work | The UX-01 criterion is that a newcomer can open the window following only the on-screen guidance; the overlay must not resize the canvas because a resize is a configuration change (closes the region); pure messages so a unit test can enumerate every reason; tab scope so a kiosk does not carry panel state into another tab | UX-01, UX-02, LOG-02 |
| 2026-09-18 | D-042 | UX-02: single-screen landing screen (two-column grid centered within 100dvh, one column below 900 px) with an inline SVG illustration (`LandingIllustration.tsx`: board divided into cells, four fingertips in slot colors, blue-bordered window, stylized face, white outside the window; no camera image, no external image); styles in `landing.css`, color, type and spacing variables declared on `:root` in `app.css` and shared with the stage; accent changed #1a73e8 → #1967d2 in CSS so links reach 4.5:1 on the surface background (the window border on the canvas keeps the compositor color); the only animation (dashed quadrilateral) is disabled under `prefers-reduced-motion`; `consentScopeNote` shows the kiosk line only when the scope is tab; the form structure and consent gate of WEB-00 are unchanged; Lighthouse is run manually with `npx lighthouse` (no added dependency, not in CI), screenshots via `npm run screenshots` into `docs/screenshots/` | D-023 requires an aesthetic landing screen without changing WEB-00 behavior; I9 forbids external resources so the illustration must be inline; contrast is measured in e2e so as not to depend on external tools | UX-02, WEB-00, UX-01, REL-01 |
| 2026-09-18 | D-043 | CLS-01: dataset mode takes images only through the `CropTap` of `restrictedFrame` (1:1 crop with holes already padded, before letterbox; types `CropMeta`, `CropTap` in `core/types.ts`), so a sample is always exactly the image the model receives and there is no path to the raw frame (I8); `src/dataset/**` is subject to boundary lint like face/ and classify/. Pure recorder (`dataset/recorder.ts`) with encoder, clock, random source and sink injected; rate follows the frame ts, one sample being encoded at a time; metadata includes `sizeClass` by short side in px (160, 320) and `position`/`edges` relative to the camera edges. Local storage: a directory via the File System Access API (Chromium) with write-through, otherwise memory and a pure stored zip (`dataset/zip.ts`, no added dependency, PNG is already compressed). The checkbox "người tham gia đã ký đồng ý" (participant has signed the consent form) must be ticked before capture and is written to `session.json`; `subjectId` is anonymous. Python tools use only the standard library (no PIL: read IHDR to check the size), the final label `labelFinal` is separate from the provisional label, split by `subjectId` with leakage checks for both session and subject, statistics written between two markers in `docs/dataset.md`; Python unittest runs in CI | CLS-01 criteria: no raw frame in the dataset, a consent form, a leakage check, statistics; the development machine and CI have neither PIL nor numpy | CLS-01, CLS-02, I8, I9 |
| 2026-09-18 | D-044 | CLS-02: build the classification pipeline before a dataset exists, with a hand-encoded stub ONNX model (`tools/make-stub-classifier.mjs`, 360 bytes, GlobalAveragePool → Flatten → Gemm: person = G − (R + B) / 2 on the normalized mean color) generated in `models:fetch`, not committed; `DEFAULTS.classifier.modelPath` points to the stub until a real model is exported (`/models/classifier.onnx`, sha256 in `models.json`). The worker initializes lazily when the region first opens (no ORT cost for a page that never opens the region; the first result is delayed by a few seconds, acceptable at 3 to 5 Hz) and loads the `onnxruntime-web/webgpu` bundle only when `navigator.gpu.requestAdapter()` returns an adapter (the headless shell has `navigator.gpu` but no adapter), otherwise `onnxruntime-web`; single-threaded wasm (no COOP/COEP); `models:fetch` copies the 4 loader files into `public/models/ort/`; the worker draws the bitmap letterboxed to the input side on its own canvas (9-argument drawImage, `check-invariants` allows this file because the worker has no path to the video). The classifier receives a second bitmap of the same crop from the builder (`copies`), one task, rate `max(250, p50)`, only when the ROI side ≥ 96 px; results pass a gate on epoch, rejected taskId, age ≤ 600 ms and region still open; a label is kept ≤ 1.5 s and attached to each face by the pure unknown rule (`subjectRule.ts`, adds `ValidatedFace.visible`); normalization `(x / 255 − 0.45) / 0.225` shared with the training script. Training, export, export check and evaluation scripts (`tools/train/`) are written for torchvision and onnxruntime Python, not yet run on the development machine; pure metrics have unittests | No real dataset yet, so no model is possible; the pipeline, gate and UI need automated testing right away with a deterministic label; avoid depending on the Python onnx package to create the stub | CLS-02, QA-02, D-013, I1 |
| 2026-09-19 | D-045 | QA-02: multi-browser benchmark `tests/bench/bench.spec.ts` with `playwright.bench.config.ts` (`npm run test:bench`): projects for the Chromium headless shell, Chrome and Edge installed on the machine via `channel` (real GPU even when headless), Firefox and WebKit when installed for Playwright; four cases (environment via `window.__wct.env` and the `env-stat` line of `debug/envProbe.ts` + `envText.ts`, mouse window with interval and result age measured by rAF inside the page, classifier forced to wasm, hands CPU then GPU on `hands.jpg`); URL parameters `hands=GPU\|CPU` and `ep=wasm`; `tools/benchmark-report.mjs` merges `reports/bench-*.json` into `docs/benchmark-matrix.json` and generates the matrix and the parameter comparison table in `docs/benchmark.md`. Parameters locked by measurement (benchmark.md section 6): `hands.delegate` = `auto` (`hands/handDelegate.ts`: GPU when WebGL runs on hardware, CPU when the renderer is software or WebGL is missing; replaces D-009); `freshness.pointMaxAgeMs` 150 ms with the GPU delegate and `pointMaxAgeMsCpu` 250 ms with CPU (the default sensitivity follows the delegate at app start and on reset); keep `faceResultMaxAgeMs` 250, `classifier.resultMaxAgeMs` 600, `labelMaxAgeMs` 1500, `hysteresisCells` 0.25, EP `['webgpu', 'wasm']` (D-013), `face.delegate` GPU. Firefox and Safari not measured (no binaries; the API and fallback table is written from MDN, the manual check procedure is in benchmark.md); the classification metrics of section 7.3 await the real model | On an RTX 3050 (Chrome, Edge 153) the hand GPU delegate runs at 28.5 to 29.5 Hz versus CPU at 10.5 to 11 Hz, on SwiftShader GPU 2 Hz versus CPU 9.5 Hz; point age needs interval p95 + inferMs p95 ≈ 80 to 100 ms (GPU) and 190 to 210 ms (CPU); face result age on receipt p95 68 ms (real GPU), 145 ms (headless), 0 over age; stub: wasm 4.7 ms is faster than webgpu 19.6 ms but S6 with MobileNetV2 was the reverse, so the EP order is kept until a real model exists | QA-02, D-009, D-013, D-040, REL-01 |
| 2026-09-19 | D-046 | LOG-02 (optional per D-022): local operations log in `src/log/` (pure `localLog.ts`: seven metadata event types `consent`, `camera-start`, `camera-stop`, `camera-error`, `reveal-open`, `reveal-close`, `config-change`; payload ≤ 1 KB JSON, otherwise `{ truncated, keys }`; at most 10 000 records or 30 days, oldest deleted first, cleanup at start and every 50 writes; the switch is off by default and saved in localStorage `wct.log`; `logOnce` for the session's consent; `idbStore.ts` IndexedDB `wct-log`, in-memory store as fallback) and `lint:boundaries` forbids `src/log/**` from importing anything outside `core/**`; StagePage builds the payload from string and number snapshots (camera, store, loop); the `LogControls` bar in the settings panel (switch, filter table by type and date, Xuất CSV (CSV export) via `Blob` + `<a download>`, Xóa (Clear)) and status in the top bar; `window.__wct.log` with `appendRaw` so e2e can test the limits | D-022 locked the log as local only, no backend (I9); the operator of a presentation machine needs to review a session (when the camera started, which source opened the region, why it closed) without exposing images: the payload is metadata only and lint blocks any path to frames; IndexedDB survives reloads, the limits prevent bloat | LOG-02, D-022, I9, UX-01 |
| 2026-09-19 | D-047 | ROI-03 (replaces D-034 and the shape part of D-038): the hand reveal region is the **convex hull** of all selected fingertips (default all five: thumb, index, middle, ring, little) of all tracked hands, no longer four slots hard-bound to hand and finger. `hands/fingertips.ts` evaluates each track × finger (uncertain, out of board, point age per sensitivity, score); an invalid point is only excluded from the hull; the region is open while ≥ `reveal.minPoints` (3) valid points from ≥ `hands.minHands` (2) hands remain, closes with `few-points` (replaces `missing-slot`) or the dominant reason among invalid points (`out-of-board` > `stale-point`; `low-score` counts as missing), `ambiguous-hands` has the highest priority. `reveal/hullSolver.ts` (replaces `quadSolver.ts`): One Euro keyed by `${trackId}:${tip}`, `convexHull` (core/cells.ts), too-small as in D-038. `RevealShape` `polygon`; `FrameOutput.points` has one element per fingertip (hand, tip, trackId, valid, reason, pStage); store `fingers` (UI `FingerControls` selects fingers, a change bumps the epoch); fingertip dots colored by hand; guidance by the number of points and hands still missing; the `fingers(...)` scenario for e2e | Requirement to track the whole hand and compute the region from all fingertips instead of two fingers per hand; the convex hull gives a simple, convex polygon for any number of points (a polygon joined by angle around the center self-intersects and is jagged with ten points); keep two hands for the framing gesture and the "drop one hand and it closes" property of INT-01; exclude points instead of closing so an occluded finger or one at the edge only shrinks the region | ROI-03, D-034, D-038, INT-01, UX-01 |
| 2026-09-19 | D-048 | REL-01 (replaces the deployment part of D-024): the public build is deployed to GitHub Pages by the `deploy` job in `ci.yml` (`needs: check`, only on push to `main`, `configure-pages@v5` → `upload-pages-artifact@v4` → `deploy-pages@v4`, Pages Source = GitHub Actions, no `.nojekyll` or `CNAME` file needed); the base path comes from the build variable `VITE_BASE` and the pure function `withBase()` in `core/config.ts` (the temporary address `https://<user>.github.io/<repo>/` needs base `/<repo>/`, repository variable `PAGES_BASE`); `public/spike-assets/` is kept for dev and local e2e but removed from `dist/` in `closeBundle`; the service worker caching models and wasm becomes mandatory (pure `public/sw.js`, cache-first for `models/*` and `assets/*` keyed by the sha256 of `models.json`, `index.html` network-first, same-origin only, no precache at install); a custom domain is bought later: A/AAAA to the GitHub Pages IPs, `www` CNAME `<user>.github.io`, Enforce HTTPS, then `PAGES_BASE` back to `/`; post-deployment checks, load measurements and the rollback procedure are recorded in `docs/deploy.md` | Anyone can open the app at one address instead of cloning the repo; the app is purely static so no server is needed; GitHub Pages reuses the existing CI (D-039), has no 25 MiB per-file limit like Cloudflare Pages (the ORT wasm is 27 MB) and attaches a domain for free; the 1 GB site and 100 GB/month limits suffice for the pilot (first load 35 to 50 MB, the service worker brings later loads to 0); the current absolute paths `/models/...` do not work under `/<repo>/` | REL-01, D-024, D-020, D-013, D-039, section 4 (Deployment), 7.27, 9.3 |
| 2026-09-19 | D-049 | UX-03 (extends UX-01, UX-02 and D-041, D-042): (1) a control token set in `app.css`: control size 32 px, button tiers (the default is the outlined secondary button, `.primary` only for the main action of each block: Bật camera, Bắt đầu, Bắt đầu thu (Start camera, Start, Start capture); `.danger` for delete; `.link` for exits; no more solid black buttons), select and inputs share the same border, checkboxes drawn as switches (still `input[type=checkbox]` inside `label.check`), fingertip chips (`label.chip`), camera status pill with a dot colored by phase (still `role=status` with the full sentence); (2) a three-zone single-line top bar at 1280 px (the brand is a link back to the landing page, camera | flexible status pill truncated with an ellipsis | Cài đặt, Debug, Trình diễn, Toàn màn hình, Thu hồi đồng ý (Settings, Debug, Present, Fullscreen, Revoke consent)), the separate "Về trang chào" (Back to landing) link is removed; (3) the settings panel is a 320 px `aside` on the right divided into vertical sections (Lưới (Grid), Cửa sổ (Window), Đầu ngón (Fingertips) and Độ nhạy (Sensitivity) only when the source is hands, Thu dữ liệu (Data capture), Nhật ký cục bộ (Local log)), sensitivity is a slider with a number input (slider `aria-hidden`, the number input keeps its aria-label for e2e), the window source is still a `<select>`; the debug panel is a drawer under the canvas (three-column mono grid, at most 150 px, scrollable), every `data-testid` keeps its content; both still collapse via `hidden` and stay in the flow (collapsing the column gives back width, opening the drawer takes height: epoch++ per D-041); (4) presentation mode `ui.present` (the Trình diễn (Present) button with `aria-pressed`, default from `?mode=present`, saved in `wct.ui`, the settings column closed on entry via URL) shares the `.overlay` layer with fullscreen: `.chrome`, `aside` and the drawer are all absolute (floating pill top center, glass sheet on the right, dark HUD top left), the canvas fills the whole `.stage` so opening a panel does not resize the canvas and does not close the window; `useIdle` (split from `useFullscreen`) hides the floating layer after 2.5 s without interaction, leaving a hint bar; the `.hud` variant of the guidance layer has large text; (5) landing screen: eyebrow, stepper, consent box with an accent border once ticked, the static SVG illustration replaced by the animated canvas `LandingPreview` (pure geometry `landingScene.ts` using `convexHull`, `rasterizePolygon` and the app's full/partial face rule; no camera, no image, no drawImage; a single static frame under `prefers-reduced-motion`); `?mode=present` is the kiosk landing screen (grid covering the whole screen with a drifting sample window, floating consent card, 56 px buttons) and then enters `#/app?mode=present`. The app default (in-flow layout) is unchanged, so older e2e only changes one assertion (collapsing the column: wider instead of taller) and the illustration selector (canvas instead of svg) | Measured on 2026-09-19 at 1280 × 720: with every panel open the canvas has 325 px left (45 %, 9 px cells), 39 controls across 7 bars without group labels, an 81 px two-line top bar, four identical black buttons for primary, secondary and delete actions; the side column returns the canvas to 960 × 522 (14 px cells) and presentation mode to 1280 × 720 (20 px cells); a floating layer that does not resize the canvas is exactly the rationale of D-041; the kiosk needs a bookmarkable address | UX-03, UX-01, UX-02, D-041, D-042, REL-01 |
| 2026-09-20 | D-050 | REL-01 deployment part in practice (supplements D-048): (1) `models.json` `ort.files` is the `asyncify` pair (bundle `onnxruntime-web/webgpu`) and the `jsep` pair (bundle `onnxruntime-web`), `wasm.files` only `vision_wasm_module_internal.{js,wasm}`; `models:fetch` copies by the list and deletes extra files; a unit test compares the loader names in the `node_modules` bundle with the manifest; `vite.config.ts` `resolve.conditions` `onnxruntime-web-use-extern-wasm` so the ORT wasm is not bundled into `dist/assets`. (2) `VITE_BASE` via `loadEnv` and `resolveBase()` (form `/` or `/name/`); `withBase()` and `modelUrls()` in `core/config.ts`, applied at the three consumers, `DEFAULTS` keeps `/models/...`. (3) The `wctBuild` plugin at `closeBundle`: removes `dist/spike-assets`, replaces the keys in `dist/sw.js`, prints sizes. (4) Pure service worker `public/sw.js` with two caches `wct-models-<sha256 models.json>` and `wct-app-<sha256 of the dist/assets list>`, keys replaced at build; `models/*`, `assets/*` cache-first, pages network-first; the cache key is the URL without its hash, looked up with `ignoreVary`; only 200 responses; `activate` deletes other `wct-*` caches and calls `clients.claim`; no precache, `StagePage` sends `warm` (page assets from Resource Timing + `modelWarmList()`); registration in `src/app/registerSw.ts` only in PROD after `load`. (5) I9 at runtime: `core/networkGuard.ts` replaces `fetch` in the three workers and the page with a same-origin-only version because `@mediapipe/tasks-vision` 1.0.1 POSTs statistics to `odml.pa.googleapis.com` every 60 s and this cannot be turned off; `check:invariants` requires every worker and `main.tsx` to install the guard; the service worker returns `Response.error()` for cross-origin requests. (6) `tests/deploy/sw.spec.ts` + `playwright.deploy.config.ts` (`npm run test:deploy`) run on `dist/` via `vite preview`, projects `chromium` and `chrome` when available, measured at the context level (`context.setOffline`, `context.on('request')`) because Playwright handles service worker requests there; results in `reports/deploy.json` go into their own section of `docs/test-report-mask.md`. (7) CI: `check` builds with `VITE_BASE` from `vars.PAGES_BASE` (default `/<repo>/`), verifies a clean build, `test:deploy`, `upload-pages-artifact` on push to `main`; job `deploy` `configure-pages` → `deploy-pages`; concurrency cancels only for PRs. `.claude/` added to `.gitignore` | Review of D-048 against the real code: the build broke classification on machines with WebGPU (missing asyncify loader), the service worker case per D-048 using `page.route`/`page.on` would be falsely green, one cache key for both models and assets bloats the cache, and MediaPipe sending telemetry violates I9 without lint noticing; measured: `dist/` 253 → 80 MB; first load caches 7 + 7 files, second load 12/12 responses from the service worker, third load still runs offline, 0 cross-origin requests on both Chromium and Chrome (WebGPU) with base `/` and `/repo/` | REL-01, D-048, D-024, D-013, D-019, sections 7.27, 9.3 |
| 2026-09-20 | D-052 | PERF-02: the loop reuses the open `RevealMask` when the controlling shape (`shapeEquals` in `core/revealShape.ts`), layout, mirror, hysteresis and `limited` are unchanged (rasterizing the same polygon with the previous mask as `prev` is a fixed point of the hysteresis rule, so the result is identical), memoizes the cell list and outline edges per mask object (`cachedCells`, `cachedOutlineEdges` in `core/cells.ts`; `FrameOutput.reveal.cells` becomes `readonly` and shared) and skips `render()` on static frames (no mask, no hand overlay, no fingertips, previous paint static, layout and grid lines unchanged); `paints` and `maskBuilds` counters in the loop snapshot | On a kiosk the region is closed most of the time; repainting a full-screen canvas at the display rate for nothing costs CPU and power. Measured with Chrome headless at 1280 × 720: main-thread share with the region closed 9.3 % → 5.3 to 6.2 %, open states unchanged. I2 keeps a single mask object per frame; `buildMask` is still the only creator | I2, D-038, D-041 |
| 2026-09-20 | D-053 | UX-04: the guidance card is compact (max 480 px, 13 px body, step dots with the label only on the current step) and collapses to one line (step dots plus title; detail and key hints hidden by CSS but kept in the DOM) after 6 s without a change of message; any change expands it; the `error` tone never collapses; `uiState.guide` (`auto`, `full`, `hidden`) in the new Interface section of the settings column, kept in the tab's `wct.ui` | User feedback on the deployed site: the card covered too much of the board (480 × 146 px, HUD 640 × 164) and stayed at full size indefinitely. Measured after (Chromium headless): 480 × 110 full, 277 × 31 collapsed, HUD 600 × 129 full and 321 × 40 collapsed | UX-01, UX-03, D-049 |
| 2026-09-20 | D-054 | I18N-01: two dictionaries in `core/i18n/` (`vi` is the reference, `Strings = typeof vi`, `en: Strings`), `t(lang)`, a tiny external `langStore`, `readLang` with the order `?lang=` → localStorage `wct.lang` → `vi`; `useLang()`/`useStrings()` in React, `LanguageSwitch` on the landing page and in the Interface section; pure text functions take `lang` with the default `vi` (`buildGuidance`, `fingertipsGuidance`, `subjectText`, `consentScopeNote`, `describeRecorder`) and the compositor receives `RenderOptions.lang` from the loop (`deps.lang`), so canvas labels follow the choice; aria-labels are translated too; the diagnostic lines of the debug drawer stay Vietnamese | The user asked for an English option for the whole interface; Vietnamese must stay the default because the e2e suite asserts the Vietnamese labels and the deployed kiosk is Vietnamese-first. No i18n library: two typed objects cost nothing at runtime and `tsc` catches a missing key. The debug lines are e2e contracts (like the `note()` strings) and developer-facing, so translating them would double the surface for no user benefit | UX-04, D-053, D-049 |
| 2026-09-20 | D-051 | Audit after REL-01: while the configured classifier model is the color stub (`isDemoClassifier()` in `core/config.ts` matches `classifier-stub.onnx`), `subjectText` appends " · demo" to every label and the face-candidate guidance states that the label comes from a color-based demo model; the landing pledge names what may be stored in the browser (consent choice, model cache, opt-in local log and dataset capture); `package.json` gets an English description, homepage and repository; `favicon.svg` and a meta description are added; the WORK-BREAKDOWN header, section 1 and 4.4 summaries and the REVIEW-ROI-01 status table are brought up to date (R-01, R-03, R-04, R-06 done; R-02, R-05 open) | The public site showed stub labels ("Người", "Hình nộm") on real faces without any hint, which misrepresents an untrained heuristic as classification; the other items are hygiene for a public repository | CLS-02, REL-01, D-044, D-050 |
| 2026-09-21 | D-055 | ROI-04: a fingertip takes part in the reveal region only while the finger is raised, decided purely from landmark geometry per track and finger (`hands/fingerPose.ts`): for the four fingers the wrist→tip / wrist→PIP ratio on the world landmarks (raised ≥ 1.05, folded < 0.9), the PIP angle (raised ≥ 135°, folded < 120°) and the tip outside the 2D palm polygon (0, 1, 5, 9, 13, 17); for the thumb the abduction ratio \|tip→little MCP\| / \|IP→little MCP\| (raised ≥ 1.0, folded < 0.85) and the palm polygon; a Schmitt band keeps the previous state, a flip needs 3 consecutive HandFrames, a new track classifies at once; the worker forwards `worldLandmarks`; a folded tip is `FingerReason` `folded` and is excluded like a stale point; the convex hull, `minPoints` 3 and `minHands` 2 of D-047 stay; the switch "Chỉ ngón đang giơ" (on by default) restores ROI-03 when off; thresholds provisional until the webcam calibration (R-02). Implemented the same day: fixture `tests/unit/fixtures/hands-pose.json`, `hands/fingerPose.ts`, `HandTrack.pose` and `landmarksWorld`, `FingerReason` `folded` (counts as `few-points`), `settings.raisedOnly`, the `raisedOnly(0|1)` scenario, fake hands with `raised`, hollow markers for folded tips, sections 7.32 and 9.1 | Measured on the spike images (`tools/probe-fingers.mjs`): the hand landmarker leaves `visibility` at 0 for every landmark, so the model gives no per-finger confidence; the geometric metrics separate raised from folded with a wide gap (ratio 1.19–1.34 vs 0.61–0.83, angle 144–175° vs 74–104°) except for the thumb, where the joint angle stays at 147° on a tucked thumb but the abduction ratio (0.75 vs 0.92–1.18) and the palm test separate it. Excluding instead of predicting keeps the ROI-03 rule that no branch keeps the region alive with invented points; the debounce keeps hull vertices from flickering while a finger is held half-bent | ROI-03, D-047, INT-01, R-02 |
| 2026-09-21 | D-056 | BRAND-01: the campaign logo is inlaid into the closed cover as cell-aligned blocks. The silhouette is the union of the three frames of the logo, hard-coded from the `<rect>` attributes of the bundled SVG `src/assets/logo-verify-human.svg` (`core/brandLogo.ts`; outer union 551.15 × 209.45 viewBox units, aspect 2.631; a unit test parses the file); the logo rectangle is `widthRatio` 0.3 of the stage width with a 2.5 % margin from the stage edge, anchored top-left and clamped into the board, so it depends on neither `cols` nor `rows`; a cell is a logo cell when at least half of its area lies under the silhouette (inclusion–exclusion over the three frames); the compositor draws white → logo layer → grid lines → video, so the grid lines cross the blocks; the loop skips the layer while a mask is open and brings it back `reappearMs` 800 after the last open frame; style `wordmark`: navy blocks with "Human" and "AI ETHIC CAMPAIGN" knocked out in white and "VERIFY:" in the brand green, derived at runtime from the same SVG (`wordmarkSvg`: frame rects dropped, fills remapped, the live Heavitas `<text>` flattened with `textLength` 268 and a fallback font stack, root `width`/`height` set to the viewBox), drawn at the fixed size and clipped to the cells; `mask/logoLayer.ts` renders one cached `OffscreenCanvas` per layout and the compositor draws it with one 9-argument `drawImage` (the module is in the invariant checker's allow-list); `ui.logo` switch "Logo trên màn che" in the Interface section, `?logo=0|1`, scenarios `logo` and `logoWordmark`, `window.__wct.logo`; e2e helpers open the stage with `logo=0` so the existing pixel cases are untouched. The plan's `inlay` behavior (only the open cells lose the logo), the center anchor and the small original logo of the mock-up were not built: the user chose hide, a corner and no small copy | The user asked for the logo inlaid into the cover with its edges on the cell boundaries as in the mock-up, hidden while the cover is open, at a size that does not change with the cell count, and provided the vector. Measured: the raster (701 × 401) and the vector agree on the three frames and the two ink colors; the union of the frames equals the ink bounding box, so the silhouette is pure geometry computable in Node; sizing from the board would still vary with the preset because the board is `cols × c` (up to one cell short of the stage), hence the stage-based size clamped into the board; coverage ≥ ½ gives the mock-up's edge on straight edges and a better corner than a center test; drawing the logo before the grid lines reproduces the lines across the blocks; keeping the video clip untouched keeps I2/I4; the wordmark is not quantized because at 64 cols the letters would be 3 to 4 cells high; the reappear delay avoids blinking on a short loss of the hands; the campaign line is live text in a font no target machine has, so a `textLength`-forced fallback keeps the layout until an outlined re-export | GRID-01, MASK-01, PERF-02, UX-04, I18N-01, I4, I9 |
| 2026-09-21 | D-057 | BRAND-01 revision (replaces the style and the open-region behavior of D-056; the geometry, the settings and the e2e arrangement of D-056 stay): the cover logo is the original artwork of `src/assets/logo-verify-human.svg` drawn as is (frames, navy text, green "VERIFY:") at the logo rectangle, not a block silhouette; it is drawn on every paint, and the reveal region cuts it cell by cell simply because the compositor draws the video in the clip of the open cells over it (white → logo → grid lines → video); closing the region brings it back on the next paint, with no hide state and no reappear delay. `logoSvg()` only flattens the live `<text>` (Heavitas, `textLength` 268, fallback font stack) and sets the root `width`/`height`; `mask/logoLayer.ts` rasterizes the crop once per layout into an `OffscreenCanvas`; the coverage rule, `logoCells`, the block colors, `reappearMs` and the `logoWordmark` scenario are removed; `window.__wct.logo` exposes the rectangle and the three frame rectangles | The user corrected the reading of the mock-up: its black region is the open reveal region (camera), the small logo in its corner is the logo itself, and "Human" is cut off where the region starts. The first implementation had inverted this (blocks shaped like the logo, hidden while open). Drawing the artwork under the existing video clip needs no new geometry, keeps I2/I4 untouched and gives the cell-aligned cut for free | D-056, MASK-01, I4 |
| 2026-09-21 | D-058 | BRAND-01 revision 2 (replaces the placement of D-056/D-057): the logo is placed on the board grid so that the borders of its three frames lie on the cell lines. The frames of the SVG sit on a 21 × 8 module grid (unit = frame width / 15; "VERIFY:" 15 × 3 from column 6, "Human" 15 × 3 from column 0, "AI ETHIC CAMPAIGN" 12 × 2 from column 9 row 6; every edge within 0.09 cell), so `logoGeometry` gives the logo 21 k × 8 k board cells with k = round(0.3 × stage width / (21 c)) ≥ 1, a margin in whole cells, clamped into the board; `mask/logoLayer.ts` draws each frame with its own `drawImage` from the frame's SVG rectangle (plus half the stroke) to its cell rectangle, so the SVG stroke is centered on the cell line and the content scales with its frame (≤ 4.8 %); the compositor draws the logo after the grid lines so the strokes cover the lines. When 21 k cells would exceed `snapMaxWidthRatio` 0.5 of the stage width (32 columns), the height cap or the board, the fixed 30 % rectangle of D-057 is used without alignment. `window.__wct.logo` reports `snapped` and `module` | The user asked for the logo's borders to coincide with the borders of the white cells. Snapping the edges of a fixed-size logo to the nearest cell lines would distort the frames by up to half a cell per edge (18 % of a frame height at 64 columns, 37 % at 32) and cut into the wordmarks, while the SVG geometry itself happens to be an almost exact 21 × 8 module grid, so scaling the logo to a whole number of cells per module keeps the artwork faithful and puts every edge on a line; the price is that the size is the nearest multiple of 21 cells (420 px on a 1280 px stage at 64, 128 and 256 columns) and that a 32-column grid cannot host the logo below 66 % of the stage, hence the fixed-size fallback there | D-056, D-057, GRID-01 |
