# Quyết định thiết kế

Mỗi dòng là một quyết định đã chốt. Thay đổi quyết định thì thêm dòng mới và ghi rõ thay thế dòng nào; không sửa dòng cũ. Mặc định đề xuất nằm ở mục 3 của [WORK-BREAKDOWN.md](./WORK-BREAKDOWN.md); số đo nằm ở [spikes.md](./spikes.md).

| Ngày | Mã | Quyết định | Lý do | Liên quan |
|---|---|---|---|---|
| 2026-09-17 | D-001 | Dùng toàn bộ mặc định ở mục 3 của WORK-BREAKDOWN.md làm điểm xuất phát, giá trị nằm trong `src/core/config.ts` | Cho phép bắt đầu ngay; tinh chỉnh ở QA-02 | Mục 3, phụ lục 9.1 |
| 2026-09-17 | D-002 | Model `.task` và wasm không commit; tải bằng `npm run models:fetch` theo `public/models/models.json` có sha256 pin ở lần tải đầu | Repo nhẹ; nguồn, phiên bản và hash vẫn được ghi rõ; chạy không phụ thuộc CDN | SETUP-00 |
| 2026-09-17 | D-003 | `src/face/**` và `src/classify/**` được import toàn bộ `src/core/**` (kiểu và toán thuần), không chỉ types và config | `faceMapping` cần `cameraToStage` trong `core/coords`; `core/` không có đường vào frame gốc | I1, mục 4.2 |
| 2026-09-17 | D-004 | Worker có tsconfig riêng (`tsconfig.worker.json`, lib WebWorker), tách khỏi app (lib DOM) | Tránh xung đột kiểu DOM/WebWorker; worker không thấy DOM | SETUP-00 |
| 2026-09-17 | D-005 | HTTPS dev bật bằng `VITE_HTTPS=1` qua `@vitejs/plugin-basic-ssl`; mặc định http trên localhost | localhost đã là secure context cho getUserMedia | CAM-01 |
| 2026-09-17 | D-006 | e2e chạy Chromium với camera giả; `--use-file-for-fake-video-capture` chỉ thêm khi có `tests/e2e/fixtures/camera.y4m` | Test chạy được ngay trước khi có TEST-00 | TEST-00, QA-01 |
| 2026-09-17 | D-007 | Khung dự án viết tay theo template Vite react-ts (không chạy `create-vite` vì thư mục đã có `docs/` và CLI hỏi tương tác) | Tái lập được, không phụ thuộc prompt của CLI | SETUP-00 |
| 2026-09-17 | D-008 | FACE-01: `face.worker.ts` là module worker (Vite bundle), GPU delegate, gọi `FilesetResolver.forVisionTasks(base, true)`; `base` khi dev là `/node_modules/@mediapipe/tasks-vision/wasm`, khi build là `/models/wasm` (hằng trong `core/config.ts`, kiểm bằng test build ở QA-01); warm-up một ảnh xám ngay sau init; nếu GPU init lỗi thì tạo lại với CPU | S1: GPU 14 ms so với CPU 39 ms mỗi ảnh, không phụ thuộc cỡ crop; module worker chỉ chạy với loader ES module; Vite dev từ chối `import()` file trong `public/`. Worker classic + `import()` động là dự phòng đã thử được | S1, FACE-01 |
| 2026-09-17 | D-009 | HAND-01: Hand Landmarker chạy trong `hand.worker.ts` (cùng mẫu module worker như D-008), nhận frame gốc qua `createImageBitmap` + transfer; delegate cấu hình được, mặc định CPU cho tay trên máy này, đo lại với webcam thật và chốt ở QA-02 | S2: chuyển frame 720p tốn 0,1 ms; detect 50 đến 70 ms rời main thread; với hai tay CPU 49 ms nhanh hơn GPU 62 ms trong worker | S2, HAND-01, PERF-01 |
| 2026-09-17 | D-010 | HAND-01: `normalizeHandedness` là hàm đồng nhất khi frame đưa vào model chưa mirror (thiết kế luôn đưa frame thô, mirror chỉ ở hiển thị); có cờ `hands.handednessSwap` để đảo nếu webcam thật cho kết quả ngược; bước đầu tiên của HAND-01 là kiểm tra 10 giây với tay phải thật | S3: tasks-vision 1.0.1 trả nhãn trùng tay giải phẫu trên ảnh chưa mirror, ngược với ghi chú cũ của tài liệu MediaPipe | S3, HAND-01 |
| 2026-09-17 | D-011 | CAM-01: video element nằm trong DOM nhưng ẩn bằng `opacity: 0; position: fixed; left: -10000px` (không dùng `display: none` dù Chromium vẫn bắn rVFC); `requestVideoFrameCallback` là nguồn `FrameStamp`, fallback rAF; `visibilitychange` sang hidden đưa vùng về Closed(tab-hidden); thêm watchdog: không có FrameStamp quá `camera.noFrameWatchdogMs` (500 ms) thì Closed(no-camera); `MediaStreamTrackProcessor` không dùng trong PoC | S4 pha 1: rVFC bắn đủ ở mọi kiểu ẩn video element; pha 2 không đo tự động được, và host có thể dừng media mà không đổi visibilityState kịp thời (pane ẩn của trình duyệt tích hợp) | S4, CAM-01 |
| 2026-09-17 | D-012 | MASK-02: giữ đường `drawImage` 1:1 vào OffscreenCanvas kích thước đúng `cameraRect`, letterbox rồi `transferToImageBitmap`, một bitmap cho mỗi worker; probe `onRestrictedFrame` (cần `getImageData`) chỉ chạy khi debug bật và giới hạn tối đa 2 Hz; dataset mode lấy bản sao qua worker hoặc theo nhịp thưa | S5: cắt, letterbox, transfer tốn 0,1 ms trên GPU thật; `getImageData` tốn 9 đến 14 ms (p95 tới 29 ms) | S5, MASK-02, TEST-00, CLS-01 |
| 2026-09-17 | D-013 | CLS-02: ONNX Runtime Web với execution providers `['webgpu', 'wasm']` (webgpu trước, wasm dự phòng); model export với input cố định 128 hoặc 160 (không dựa vào dynamic axes); loader ORT theo môi trường như D-008; xem xét COOP/COEP để bật wasm đa luồng chỉ khi cần | S6: wasm đơn luồng 59 ms, webgpu 19 ms cho MobileNetV2 224 trên RTX 3050; model zoo cố định 224 nên 128 lỗi | S6, CLS-02 |
| 2026-09-17 | D-014 | Mã spike nằm ở `tools/spikes/` chạy trên dev server; runner headless `tools/spikes/run-spike.mjs` chỉ cho phép đo không liên quan tab nền; asset spike trong `public/spike-assets/` không commit | Playwright tắt throttling nền; môi trường tool không spawn được `chrome.exe`; pane trình duyệt tích hợp có thể ẩn | SPIKE-00 |
| 2026-09-17 | D-015 | Thêm lớp nền tảng web: backend Node.js + Fastify + TypeScript trong `server/`, cơ sở dữ liệu SQLite qua `node:sqlite` cho PoC (Postgres khi triển khai nhiều người, cùng lớp `Repo`), frontend có định tuyến `/`, `/app`, `/admin`; một tiến trình Node phục vụ cả `dist/` và `/api` sau reverse proxy HTTPS | Yêu cầu mới: backend, frontend, cơ sở dữ liệu rõ ràng; `node:sqlite` có sẵn trong Node 24, không cần native module | A11, mục 4.7, API-00, DEP-01 |
| 2026-09-17 | D-016 | Người vào web dùng phiên ẩn danh (cookie `sid` ký, httpOnly, SameSite=Lax), tên hiển thị tùy chọn; phải đồng ý ở trang `/` (văn bản có phiên bản) mới vào `/app`; `getUserMedia` chỉ gọi sau `assertCameraAllowed()` và từ hành động bấm | Yêu cầu giao diện đơn giản trước khi cho dùng camera; bất biến I10 | WEB-00, CAM-01 |
| 2026-09-17 | D-017 | Nhật ký chỉ metadata (danh sách loại sự kiện trong `shared/events.ts`, payload ≤ 2 KB, ≤ 50 sự kiện mỗi lần gửi); server từ chối phần còn lại; giữ 90 ngày; IP có thể che octet cuối; không bao giờ nhận frame, crop, landmark | Bất biến I9; yêu cầu admin xem log người vào web mà không phá quy tắc riêng tư của kế hoạch gốc | LOG-01, ADM-01, SEC-01 |
| 2026-09-17 | D-018 | Admin đăng nhập bằng tài khoản trong bảng `users` (role admin, scrypt), cookie `adm` 12 giờ, tạo lần đầu từ `ADMIN_USERNAME`/`ADMIN_PASSWORD`; chống CSRF bằng SameSite + kiểm tra `Sec-Fetch-Site`/`Origin`; giới hạn 120 yêu cầu/phút và 5 lần đăng nhập/phút | Không phụ thuộc dịch vụ ngoài; đủ cho PoC và pilot nội bộ | ADM-01, SEC-01 |
| 2026-09-17 | D-019 | Thay D-015, D-016, D-017, D-018: quay về ứng dụng tĩnh thuần client như kế hoạch gốc. Không có backend, cơ sở dữ liệu, nhật ký người vào web, trang admin hay telemetry; hosting là static HTTPS. Giữ trang chào `/` với nút đồng ý (văn bản có phiên bản, lưu `localStorage` khóa `wct.consent`) và sân khấu `/app`; `getUserMedia` vẫn chỉ gọi sau `assertCameraAllowed()` (I10). Mã backend, admin, telemetry và test của chúng chuyển vào `archive/backend/`, nằm ngoài build, lint và test; xóa sau khi đã commit nếu không cần | Yêu cầu được xác nhận lại: chỉ là web client, không cần thống kê người dùng; backend làm client phụ thuộc một tiến trình nó không cần và kéo theo vận hành, bảo mật, riêng tư (IP, user-agent) mà kế hoạch gốc cố tránh | A11, A12, mục 4.7, WEB-00 |
| 2026-09-17 | D-020 | Định tuyến bằng `HashRouter` (`/#/app`) thay `BrowserRouter`; `cameraGateReason()` kiểm đường dẫn theo hash (`currentRoute()` trong `gate.ts`) | `dist/` chạy trên mọi host tĩnh, kể cả GitHub Pages, không cần SPA fallback; không có máy chủ nào để cấu hình rewrite (D-019) | WEB-00, REL-01, mục 4.7 |
| 2026-09-17 | D-021 | Phạm vi đồng ý cấu hình được ở `DEFAULTS.consent.scope`: `device` (localStorage, giữ qua các lần mở, mặc định) hoặc `tab` (sessionStorage, hết hiệu lực khi đóng tab, cho kiosk và trình diễn); nút Thu hồi đồng ý giữ ở cả hai | Máy trình diễn nhiều người dùng chung không được kế thừa đồng ý của người trước | WEB-00, UX-02 |
| 2026-09-17 | D-022 | Không có nhật ký người vào web. Nếu cần nhật ký vận hành thì làm gói tùy chọn LOG-02: sự kiện metadata ghi IndexedDB của chính trình duyệt, bảng xem tại chỗ và xuất CSV; không có gì rời trình duyệt (I9). Nếu cần thống kê truy cập nhiều máy thì dùng thống kê của nhà cung cấp host tĩnh, không viết mã | Thay vai trò của LOG-01 và ADM-01 đã bỏ mà không cần backend | LOG-02, I9 |
| 2026-09-17 | D-023 | Trang `/` là màn hình bắt đầu của web, thiết kế thẩm mỹ ở gói UX-02: tên, giới thiệu ngắn, cam kết riêng tư, đồng ý và nút Bắt đầu trên cùng một màn; vẫn không gọi camera, không gọi mạng; cấu trúc form, `CONSENT_VERSION` và cổng đồng ý của WEB-00 giữ nguyên, e2e của WEB-00 phải pass sau khi thiết kế lại | Yêu cầu: trang đồng ý ban đầu trở thành màn hình bắt đầu mang tính thẩm mỹ | UX-02, WEB-00, I10 |
| 2026-09-17 | D-024 | REL-01 gồm: CI chạy `models:fetch`, `lint`, `lint:boundaries`, `check:invariants`, `test:unit`, `test:e2e`, kiểm Mermaid, `build` rồi đẩy `dist/` lên host tĩnh HTTPS; service worker cache model và wasm là tùy chọn (kiosk, mở lần sau nhanh, offline); `archive/backend/` commit vào nhánh riêng `archive/backend` ở lần commit đầu và không nằm trên `main` | Đóng gói tĩnh thuần client; giữ `main` sạch | REL-01 |
| 2026-09-17 | D-025 | CAM-01: `CameraSource` nhận `gate` (app truyền `assertCameraAllowed`) và gọi đồng bộ ngay trước mỗi `getUserMedia`, kể cả khi đổi camera hay thử lại; `getUserMedia` chỉ xuất hiện ở `src/camera/cameraSource.ts` (kiểm bằng `tools/check-invariants.mjs`). `frameId` tăng đơn điệu trong suốt đời `CameraSource`, không reset khi đổi camera. Máy trạng thái thuần ở `cameraState.ts` (idle, requesting, active, ended, error); `stop()` về idle; watchdog và tab ẩn là cờ `stalled`, `hidden` trong snapshot, không phải trạng thái; `cameraCloseReason()` suy ra `no-camera` hoặc `tab-hidden` cho vòng lặp. `epoch.bump()` mỗi lần vào requesting và mỗi lần rời active | I10 buộc mọi đường tới `getUserMedia` đi qua cổng; frameId không trùng là tiêu chí của CAM-01; tách máy trạng thái thuần để unit test trong Node | CAM-01, D-011, I10 |
| 2026-09-17 | D-026 | GRID-01: `computeLayout(stage, grid, cam)` nhận kích thước camera và `Layout` mang `cam`; khi camera chưa active, store dùng kích thước camera mặc định (`DEFAULTS.camera`, 1280 × 720) để lưới hiện trước khi bật camera, rồi tính lại kèm epoch++ khi camera active với kích thước thật. `camVisibleRect` giữ số thực; `windowToCameraRect(win, layout, mirror)` làm tròn từng cạnh (`Math.round`) rồi clip, nên cửa sổ kề nhau chung cạnh ở cả hai không gian; compositor và restrictedFrame dùng đúng rect này. Vạch lưới `#e6e6e6`, 1 px thiết bị tại mọi ranh giới ô kể cả biên bảng (vạch biên phải và dưới nằm trong bảng); e2e coi trắng và màu này là "không có pixel camera". Bật/tắt vạch lưới không đổi epoch; cols, rows, mirror, kích thước stage, kích thước camera đổi thì epoch++. Một bộ đếm epoch dùng chung cho camera và store sân khấu (`src/loop/store.ts`). Cài đặt lưới chưa lưu giữa các lần mở (UX-01 xét) | Kích thước camera là đầu vào bắt buộc của `scale` và `camVisibleRect` mà chữ ký ở mục 4.1 chưa có; lưới phải hiện ngay khi vào `#/app` (bước 1 của trải nghiệm); một epoch cho mọi nguồn thay đổi (A7) | GRID-01, mục 4.1, MASK-01 |
| 2026-09-17 | D-027 | ROI-00: `WindowSource.current(now)` trả `{ window, limited }` đã kẹp trong bảng hoặc `{ window: null, reason }`. Chuột: bấm trên bảng mở cửa sổ tại con trỏ (n mặc định `reveal.mouseInitialN` = 8), kéo để dời, lăn đổi n quanh tâm, Esc đóng, Space mở lại ở tâm cũ hoặc giữa bảng; tọa độ CSS đổi sang px stage theo tỷ lệ `canvas.width / rect.width`; layout đổi thì đặt lại cửa sổ từ tâm px stage nên cửa sổ giữ chỗ trên màn hình. `buildMask` (bước 1 của MASK-01) làm ở ROI-00 vì viền cửa sổ và RevealState cần mask. Vòng lặp `frameLoop` chạy bằng rAF (khung của mục 4.4: WindowSource, buildMask một lần, RevealState, compositor); INT-01 nối các pipeline. Máy trạng thái ở `core/revealState.ts`: closed → open tăng epoch; open → closed tăng với tab-hidden, no-camera, user; config-changed không tăng ở đây vì store đã tăng đúng một lần khi đổi cấu hình; lý do do tay không tăng. Đóng vì camera dừng hoặc tab ẩn nối ở INT-01 (ROI-00 phải kiểm được hình học khi chưa bật camera). Viền cửa sổ 2 px `#1a73e8`, đỏ `#d93025` khi limited, vẽ trong stageRect. Đổi nguồn cửa sổ không tăng epoch, vòng lặp tự đóng cửa sổ | Tiêu chí ROI-00: kiểm mọi hình học bảng và cửa sổ không cần tay (và không cần camera); một epoch cho mọi nguồn thay đổi, không tăng hai lần cho cùng một nguyên nhân | ROI-00, MASK-01, mục 4.6, 5.7 |
| 2026-09-17 | D-028 | MASK-01: `render(ctx, layout, { showLines, mirror, drawable, mask })` trong `mask/compositor.ts` là nơi duy nhất drawImage lên output: nền trắng → vạch lưới → (chỉ khi có mask và `drawable`) `drawImage(cameraRect → stageRect)` trong `ctx.clip(stageRect)`, mirror bằng `translate(s.x + s.w, s.y)` + `scale(-1, 1)` chỉ trong phạm vi stageRect → viền. `drawable` là `FrameSource.drawable` (video ẩn hoặc canvas tổng hợp), null khi camera chưa active: cửa sổ mở chỉ có viền. Mỗi frame vẽ lại toàn bộ, không tích lũy. `check:invariants` kiểm `drawImage(` chỉ ở `compositor.ts` và `restrictedFrame.ts` và luôn 9 tham số (có rect nguồn). E2E MASK-01 dùng camera giả của Chromium vì TEST-00 mới có nguồn tổng hợp: dừng video rồi so output với ảnh tham chiếu vẽ trong trang từ cùng cameraRect và mirror, sai khác ≤ 8 mức mỗi kênh; TEST-00 thêm kiểm màu tuyệt đối. Đóng vùng khi camera dừng hoặc tab ẩn vẫn để INT-01 | Bất biến I2 (một mask) và I4 (trắng trước video): không có đường nào vẽ frame toàn khung; kiểm bằng script để giữ khi thêm gói sau | MASK-01, TEST-00, INT-01 |
| 2026-09-17 | D-029 | TEST-00: nguồn tổng hợp và probe bật bằng query trong hash (`#/app?debug=1` bật probe `window.__wct.probes` và kịch bản `window.__scenario`; thêm `&source=synthetic` thay camera bằng `SyntheticCameraSource`, không gọi `getUserMedia`, nút Bật camera bị vô hiệu). Kịch bản điều khiển cửa sổ qua `MouseWindowSource` (`setWindow`, `moveWindow`, `resizeWindow`, `close`) thay vì một nguồn cửa sổ riêng; `delayWorker(ms)` ghi `probes.workerDelayMs` cho FACE-02. Cảnh mặc định: nửa trái camera xanh lá `#00ff00`, nửa phải magenta `#ff00ff`, không có mặt; e2e kiểm màu tuyệt đối trong cửa sổ. Probe output đọc ngược theo nhịp (mặc định 250 ms) và chỉ khi có listener; probe restricted tối đa 2 Hz, được MASK-02 gọi. Clip y4m tạo bằng `tools/make_test_clips.py` (cần ffmpeg, mặc định 640 × 480, 20 fps, 4 s ≈ 37 MB, không commit); máy hiện tại không có ffmpeg nên chỉ kiểm `--dry-run` | Không thêm nhánh mã sản phẩm cho test; mọi móc debug nằm sau `?debug=1` và không có gì rời trình duyệt (I9); y4m 720p 10 s quá lớn (400 MB) | TEST-00, MASK-02, FACE-02 |
| 2026-09-17 | D-030 | MASK-02: `createRestrictedFrameBuilder({ probes })` trong `mask/restrictedFrame.ts` là nơi duy nhất tạo `RestrictedFrame`; `build(source, mask, stamp, epoch, taskId, copies)` nhận `FrameSource` (không nhận drawable rời, nên nguồn chỉ có thể là video ẩn hoặc canvas tổng hợp, không bao giờ là canvas output; `check:invariants` cấm `document`, `querySelector`, `getElementById`, `HTMLCanvasElement` trong file này và đòi `source.drawable`) và trả `{ kind: 'ok', frames }` (một bitmap mỗi worker: vẽ lại letterbox rồi transfer lần nữa), `too-small` (cạnh ngắn `cameraRect` < `face.minRoiPx`, không động tới canvas, không tiêu taskId) hoặc `no-frame`. Hai canvas tái sử dụng: crop đúng cỡ `cameraRect` (chỉ đổi width/height khi cỡ đổi), letterbox `face.inputSize` fill xám `face.padGray`. Letterbox: `scale = size / max(w, h)`, `dx, dy = floor((size − cạnh·scale) / 2)`, kích thước đích giữ số thực; toán ở `core/letterbox.ts` để FACE-02 ánh xạ ngược mà không import `mask/`. Vòng lặp gọi build ở bước 6 sau khi vẽ output, khi vùng mở, nguồn có frame mới (`frameId` khác lần trước) và tới nhịp `face.targetHz`; chưa có worker nên bitmap được đóng ngay; `taskId` tăng từ 1 chỉ khi ok. Probe nhận `ImageData` của ảnh letterbox ngay trước transfer theo nhịp của probes. E2E gate cứng dùng bảng màu có bất biến kênh (trong cửa sổ xanh dương, ngoài magenta, đệm xám: pixel hợp lệ luôn có r = g) và đối chứng thu vùng người 1 px để chứng minh phép đo thấy sai lệch 1 px | Gate cứng 7.2 phải đo tại probe với nguồn không thể là output; tách toán letterbox khỏi canvas để face/ dùng được (I1); không tạo tác vụ rỗng khi quá nhỏ (mục 5.9) | MASK-02, FACE-02, INT-01, mục 5.9 |
| 2026-09-17 | D-031 | FACE-01: giao thức ở `face/faceProtocol.ts` (chỉ import `core/`): `init`, `detect` (mang `RestrictedFrame`, transfer `input`), `ready`, `result`, `error` (`taskId` null là lỗi init). `face.worker.ts` là module worker, `forVisionTasks(base, useModuleLoader)` rồi tạo với GPU, lỗi thì tạo lại với CPU, warm-up ảnh xám `face.inputSize`, `input.close()` trong `finally`. `FaceClient` là nơi duy nhất tạo worker mặt, `start()` gọi lúc mount `StagePage`; `submit()` luôn nhận quyền sở hữu bitmap (transfer hoặc đóng ngay khi drop); `rejectAll()` đánh dấu mọi taskId đã gửi, busy giữ tới khi worker trả về; nhịp gợi ý `minIntervalMs = max(1000 / face.targetHz, p50 inferMs)` trên 20 mẫu gần nhất (targetHz giữ 12 theo D-001, không phải 15). Vòng lặp: bật `accepting` khi vùng mở, gọi `rejectAll` và tắt khi đóng hoặc khi mask mở lại với epoch mới; tạo buffer khi worker rảnh hoặc probe muốn đọc; đếm `faceDetectSubmitted` khi gửi được và `faceDetectDropped` cho mọi buffer không gửi được (chưa sẵn sàng, bận). Debug: `window.__wct.face.snapshot()`, thanh debug và thumbnail buffer vừa gửi vẽ bằng `putImageData` từ probe `onRestrictedFrame` (chỉ `?debug=1`). E2E chạy worker thật trong Chromium headless, chấp nhận GPU hoặc CPU, chờ ready tối đa 60 s; ngưỡng mỗi test e2e nâng từ 30 lên 60 s vì mọi trang đều khởi tạo worker mặt song song | Một worker nhận đúng một loại message mang ảnh (I1); không xếp hàng tác vụ (mục 5.10); rate không vượt tốc độ suy luận thực | FACE-01, FACE-02, INT-01, mục 4.5, 5.10 |
| 2026-09-17 | D-032 | FACE-02: `validateFace(result, task, ctx)` trong `face/faceValidate.ts` (chỉ import `core/` và file cùng thư mục) trả `rejected` với lý do `epoch`, `rejected-task`, `stale` (tuổi tính từ `ts` của frame gửi, ngưỡng `freshness.faceResultMaxAgeMs`), `no-mask`, hoặc `ok` với `ValidatedFace[]` và số mặt bị bỏ; lề full là `face.fullFaceMarginRatio` × cạnh ngắn ROI; landmark sang stage bằng layout hiện tại (đổi layout thì epoch đổi và kết quả bị loại trước đó, nên đó cũng là layout của epoch tác vụ). Toán hình chữ nhật ở `core/rect.ts`, ánh xạ ở `face/faceMapping.ts`. Vòng lặp: kết quả về qua `FaceClient.subscribeResults`, validate ngay lúc về; danh sách mặt giữ tới kết quả kế (kết quả 0 mặt cũng thay), xóa khi đóng hoặc mở lại với epoch mới, và hết hạn sau 4 × `faceResultMaxAgeMs` không có kết quả mới để overlay không đứng hình khi worker ngừng trả. Overlay vẽ trong `ctx.clip(stageRect)` sau video và trước viền: bbox full `#188038` nét liền, partial `#f9ab00` nét đứt, landmark chấm 2 px. Trạng thái `covered`, `too-small` (cạnh ngắn `cameraRect` < `face.minRoiPx`, tính từ mask hiện tại), `partial-face`, `face-candidate`, `searching`; `FrameOutput` phát mỗi frame qua `loop.events` (CustomEvent `frame`) và có trong `snapshot().output`; `points` để trống tới HAND-02. Kịch bản `delayWorker` trễ ở `FaceClient.resultDelayMs` (busy giữ trong lúc trễ) thay vì trong worker. E2E có mặt thật chạy cục bộ với `public/spike-assets/face.png` (asset spike, không commit), tự bỏ qua khi thiếu file; e2e không cần ảnh kiểm `searching`, `covered`, sự kiện `frame` | Cây quyết định 5.10 và tiêu chí FACE-02: che lại thì mặt biến mất ngay frame kế, dời cửa sổ không kéo mặt theo, partial không có landmark dưới vùng trắng, kết quả về sau khi đóng không hiện; tránh overlay đứng hình khi worker chậm | FACE-02, INT-01, UX-01, mục 4.4, 4.6, 5.10 |
| 2026-09-17 | D-033 | HAND-01: giao thức ở `hands/handProtocol.ts` (`init`; `detect` mang `HandRawFrame` = bitmap toàn khung chưa mirror kèm `frameId`, `ts`, `epoch`, transfer; `ready`, `result`, `error` với `frameId` null là lỗi init). `hand.worker.ts` là module worker VIDEO mode, `numHands` 2, delegate theo `hands.delegate` (CPU, D-009) lỗi thì đổi delegate còn lại, warm-up ảnh xám, timestamp không bao giờ lùi, `input.close()` trong finally. `HandClient` (`hands/handClient.ts`) là nơi duy nhất gọi `createImageBitmap` trong `src/` (`check:invariants` kiểm, I1): một frame chạy tại một thời điểm, frame mới trong lúc bận bị bỏ (không xếp hàng), start lại được sau dispose (React StrictMode mount lại). `normalizeHandedness(label, { inputMirrored, swap })` ở `hands/handLandmarker.ts` là hàm đồng nhất với frame thô (D-010), đảo theo `settings.handednessSwap` (cờ mới trong `StageSettings`, checkbox "Đảo trái/phải", không đổi epoch, đổi thì tracker reset). `HandTracker` (`hands/handTracker.ts`, thuần): chi phí = khoảng cách tâm lòng bàn tay (trung bình landmark 0, 5, 9, 13, 17) / bề rộng camera + `handednessPenalty` 0,1 khi nhãn khác (nhỏ hơn `matchCostMax` 0,15 để nhãn nhấp nháy một frame không tách track; nhãn ngược liên tiếp `relabelFrames` = 3 thì track đổi handedness giữ id; `score` = 1 − score khi nhãn ngược); ghép tham lam theo chi phí tăng dần; `uncertain` khi một detection hoặc một track có hai ứng viên chênh nhau dưới `ambiguityDelta` 0,03: giữ nguyên track, không cập nhật `lastSeenTs`, không tạo track mới; track bị xóa khi quá `trackDropMs` 150 ms kể từ lần thấy cuối VÀ vắng trong ít nhất `trackDropFrames` = 2 lần cập nhật liên tiếp (với pipeline 20 Hz bằng đúng quy tắc 150 ms của kế hoạch; với pipeline chậm hơn 150 ms mỗi frame, một lần bỏ lỡ không xóa track; frame uncertain tính là vắng); đổi kích thước bitmap thì reset; id tăng đơn điệu, không dùng lại; `HandFrame` trả bản sao. `HandFrame` giữ ở `HandPipeline` (`hands/handPipeline.ts`, `latest`; đọc qua `loop.snapshot().hands` và `window.__wct.hands`) thay vì `StageStore` để không re-render React 20 Hz. Vòng lặp feed pipeline chỉ khi `settings.windowSource` là `hands` (worker tay khởi tạo lười ở lần feed đầu nên chế độ chuột và e2e cũ không tốn thêm); rời nguồn tay thì reset; chưa có `HandWindowSource` nên nguồn tay đóng với `missing-slot` tới INT-01. Overlay debug tay (`compositor.drawHands`) vẽ sau viền trên nền trắng, cả ngoài `stageRect`: bbox, năm đầu ngón, tâm, nhãn "Trái #id"/"Phải #id", mờ khi cũ hơn `pointMaxAgeMs`, nét đứt khi uncertain. Chỉ số landmark ở `core/handLandmarks.ts`; `rectCameraToStage` chuyển về `core/coords.ts`. Bước kiểm 10 giây với tay phải thật chưa làm được trên máy phát triển (môi trường công cụ không có webcam): người vận hành làm thủ công, nhãn ngược thì bật "Đảo trái/phải". E2E có tay thật chạy cục bộ với `public/spike-assets/hands.jpg` (S3), tự bỏ qua khi thiếu; trong Chromium headless CPU delegate ổn định khoảng 100 ms mỗi frame (vài frame đầu tới một giây, GPU/SwiftShader 540 ms) nên test chờ ổn định và đo theo số kết quả; cảnh tổng hợp của các ca tay chạy 8 fps và các ca chờ worker (mặt, tay) sẵn sàng chờ tới 90 đến 120 s vì cả bộ chạy song song làm khởi tạo chậm hơn một phút, Playwright giới hạn 6 worker (máy 16 luồng), thanh debug cố định một dòng để chữ trạng thái đổi không làm canvas đổi cỡ và epoch tăng giữa test, camera.spec đo fps ổn định giữa hai lần đọc thay vì từ frame đầu; chuyển động của ảnh trong `SyntheticCameraSource` tính từ lúc đặt ảnh để bật chuyển động sau khi pipeline ổn định mà không nhảy | Tiêu chí HAND-01 (id không đổi khi hai tay di chuyển bình thường, hai tay chéo cho uncertain thay vì hoán đổi id), D-009, D-010, mục 4.4 bước 2, 5.4, 5.8; tránh worker tay chạy khi không dùng | HAND-01, HAND-02, INT-01, PERF-01 |
| 2026-09-17 | D-034 | HAND-02: `hands/slots.ts` thuần, `evaluateSlots(frame, configs, layout, mirror, now)` đánh giá lại bốn slot ở mỗi frame render từ `HandFrame` mới nhất (không lưu slot riêng): track cùng tay có score cao nhất, không bao giờ lấy tay khác; landmark tip đã là px camera → `cameraToStage`; lý do theo ưu tiên mục 5.8 `ambiguous-hands` → `missing-slot` → `out-of-board` → `stale-point` (tuổi = now − `lastSeenTs` của track, ngưỡng `freshness.pointMaxAgeMs`), thêm `low-score` khi score < `hands.minTrackScore` = 0,5 (đóng vùng với `missing-slot`). `settings.slots` trong `StageStore` với preset `DEFAULTS.slots`; đổi slot thì `epoch++` không tính lại layout và cửa sổ đang mở đóng với `config-changed` (UC-03); UI `SlotControls` (tay và ngón cho từng slot) chỉ hiện khi nguồn cửa sổ là tay. Overlay `compositor.drawSlots`: chấm 8 px màu theo slot (`SLOT_COLORS`) kèm số, mờ (alpha 0,4) khi không hợp lệ còn điểm, ẩn khi không có điểm, vẽ sau overlay tay. `FrameOutput.points` điền mỗi frame khi nguồn là tay, rỗng với chuột; `LoopSnapshot.slots` cho debug và e2e; `statusMessage(output, slots)` khi vùng đang che với nguồn tay trả hướng dẫn `slotsGuidance` ("Đưa hai tay vào khung hình: thiếu …", kèm cũ, ngoài bảng, chưa rõ tay, hai tay chéo nhau). Chưa có solver (ROI-01) nên nguồn tay đóng với `slotsCloseReason`, và `missing-slot` khi đủ bốn slot. Trong headless CPU, tuổi điểm lúc render xấp xỉ 100 đến 150 ms nên e2e chấp nhận `stale-point` xen kẽ hợp lệ và chỉ coi `missing-slot` là mất tay | Tiêu chí HAND-02: mất một ngón thì slot không hợp lệ trong tối đa 150 ms cộng một frame render (đánh giá mỗi frame theo lần thấy cuối); không slot nào tự chuyển sang tay khác (chỉ ghép cùng handedness); mục 5.8, UC-03, UC-15 | HAND-02, ROI-01, INT-01, UX-01 |
| 2026-09-18 | D-035 | ROI-01: `reveal/oneEuro.ts` thuần (`stepOneEuro(state, x, ts, params)`, trạng thái là record bất biến, mẫu đầu không trễ, dt ≤ 0 bỏ qua) và `reveal/squareSolver.ts` thuần (`solveSquare(points, layout, prev, ts, opts)`): tâm = trung bình bốn điểm, cạnh = min(bboxW, bboxH) px stage, lọc One Euro cho cx, cy, cạnh; `sideCells < nMin` → `too-small` (có hysteresis khi đang mở: chỉ đóng khi xuống dưới `nMin − h`, mở lại cần đủ `nMin`); n rồi col, row từ tâm làm tròn với hysteresis `h = 0,25` ô (`snapWithHysteresis`: chỉ đổi khi vượt ranh giới làm tròn quá h) so với cửa sổ **yêu cầu** (chưa kẹp) của lần trước; n đổi thì col, row làm tròn lại quanh tâm (cửa sổ phóng quanh tâm); `clampWindow` kẹp vào bảng giữ vuông và `limited` = cửa sổ yêu cầu khác cửa sổ đã kẹp, nên `limited` giữ nguyên chừng nào tay còn đòi cửa sổ tràn bảng. `HandWindowSource` (`reveal/handWindowSource.ts`) là `WindowSource` của nguồn tay: mỗi `current(now)` đánh giá bốn slot (`hands/slots.ts`, tuổi điểm theo độ nhạy), thiếu thì trả lý do mục 5.8 kèm `slots`, đủ bốn thì giải đúng một lần cho mỗi `HandFrame` mới với mốc `frame.ts` (One Euro lọc theo nhịp kết quả tay, không theo nhịp render); trạng thái solver reset khi closed → open (mục 4.6), khi layout, mirror hay cấu hình slot đổi identity, và khi `reset()`. `WindowSample` có thêm `slots` nên vòng lặp không tự đánh giá slot nữa; lý do đóng của nguồn tay đi thẳng vào `RevealState` (không đổi epoch, D-027) | Tiêu chí ROI-01: tay giữ yên thì cửa sổ không nhấp nháy (unit: rung ±3 px quanh ranh giới ô không đổi ô; hysteresis 0 thì nhấp nháy), đổi n mượt, sát mép vẫn vuông và có `limited`; mục 5.8, 4.6 | ROI-01, INT-01, QA-02 |
| 2026-09-18 | D-036 | ROI-01 bước 4 (UC-09): độ nhạy trong `StageStore.settings.sensitivity` (`reveal/sensitivity.ts`: `minCutoff`, `beta`, `hysteresisCells`, `nMin`, `pointMaxAgeMs`, mặc định từ `core/config.ts`, kẹp theo `SENSITIVITY_LIMITS`), thanh `SensitivityControls` chỉ khi nguồn là tay, áp dụng ở frame kế, không đổi epoch, nút "Đặt lại độ nhạy"; `statusMessage` nêu "chạm mép bảng (bị kẹp)" khi `limited` và "quá gần nhau: tách hai tay ra" khi `too-small`. Debug (TEST-00): `HandPipeline.setFake(gen)` thay worker bằng `HandFrame` giả lập sinh mỗi lần `feed(source, epoch, now)` (worker không được start), kịch bản `window.__scenario.hands(spec)` với `debug/fakeHands.ts` (21 landmark, đầu ngón cái tại (x, y + spread/2), trỏ tại (x, y − spread/2), rung giả ngẫu nhiên lặp lại theo frameId, `ageMs`, `uncertain`); `window.__wct.handWindow.snapshot()` và dòng `solver-stat` cho e2e. Trong headless CPU, tuổi điểm thật xấp xỉ 100 đến 200 ms nên e2e tay thật nâng "Tuổi điểm" lên 1000 ms để cửa sổ mở ổn định; e2e tay thật không còn khẳng định vùng đóng khi đủ tay | e2e cần kiểm solver và cửa sổ theo tay trong CI mà không có ảnh tay thật hay webcam; UC-09 đòi chỉnh lúc chạy | ROI-01, QA-01, QA-02, UX-01 |
| 2026-09-18 | D-037 | INT-01: `loop/closeGate.ts` với `CloseGate { reason(), subscribe() }`: `cameraGate(camera)` theo `cameraCloseReason` của snapshot camera (tab ẩn ưu tiên, rồi no-camera khi chưa active, đang đổi camera hay watchdog), `visibilityGate(document)` cho nguồn tổng hợp (chỉ tab ẩn). Vòng lặp hỏi `reason()` mỗi frame trước nguồn cửa sổ (gate đóng thì không hỏi nguồn, slot rỗng) và còn đăng ký `subscribe` để `closeNow(reason)` ngay trong sự kiện: đóng, xóa mặt, `rejectAll`, `accepting = false`, vẽ trắng, phát `FrameOutput`, vì tab ẩn thì `requestAnimationFrame` không chạy và kết quả mặt về sau phải bị loại theo epoch mới. Camera dừng hay đổi camera tăng epoch hai lần (CameraSource khi rời active và `stepReveal` với no-camera): chấp nhận, epoch chỉ cần tăng. Đổi nguồn cửa sổ là đổi cấu hình: `StageStore` tăng epoch (không tính lại layout), vòng lặp đóng với `config-changed` không tăng thêm (D-027); cửa sổ chuột giữ trạng thái mở về logic nên chuyển về chuột hay hết gate thì mở lại ở frame kế (closed → open, epoch++). `statusMessage` nêu tab đang ẩn hoặc camera chưa cấp frame. Chưa nối ClassifierClient (CLS-02). Hệ quả cho e2e: cửa sổ chuột chỉ mở khi camera cấp frame nên các ca ROI-00 và kịch bản `windowAt` với camera thật bật camera giả trước (`startFakeCamera`) và không kiểm pixel trắng trong cửa sổ nữa (trong cửa sổ là video, mask.spec kiểm); mask.spec dừng video để so pixel thì so ngay trong trang rồi phát lại, vì quá 500 ms watchdog đóng vùng. Bước 4 (chạy toàn bộ mục 7 bằng tay thật trên máy phát triển) chưa làm được vì môi trường công cụ không có webcam: cần một lần chạy thủ công cùng bước kiểm D-010 | Mục 4.6 và UC-15: tab ẩn, camera dừng, đổi cấu hình phải đóng vùng và loại tác vụ đang chạy; INT-01 bước 3 | INT-01, QA-01, UX-01, CLS-02 |
| 2026-09-18 | D-038 | ROI-02 (thay phần hình vuông của D-035 và điểm "bốn điểm là bộ điều khiển" ở mục 2.1): vùng mở theo tay là **tứ giác có bốn đỉnh là bốn đầu ngón**, mask là **tập ô** giao với tứ giác, kể cả ô chỉ bị cạnh cắt qua (chốt hình vuông ở kế hoạch 14/09 là sai sót). `RevealShape` = cửa sổ vuông (chuột) hoặc `quad` (px stage, đã sắp đỉnh theo góc quanh tâm thành đa giác đơn); `RevealMask` = `box` (hộp bao theo ô) + `cells` (bitmap ô) + `cellCount` + `stageRect`, `cameraRect` (rect nguyên của hộp bao) + `holesCam` (rect camera nguyên của từng ô trong hộp mà không mở). `core/cells.ts` thuần: `polygonOverlapsRect` (phần chung diện tích dương: đỉnh trong ô, góc ô trong đa giác, cạnh đi qua phần trong ô; chạm cạnh không tính; góc lùi 1e-6), `rasterizePolygon` với hysteresis theo ô (ô đang mở giữ khi còn chạm ô nới rộng h·c, ô tắt chỉ bật khi lấn vào ô thu hẹp h·c; h = `sensitivity.hysteresisCells`), `cellOutlineEdges`. `reveal/quadSolver.ts` thay `squareSolver`: One Euro từng tọa độ (8 bộ lọc), `too-small` khi cạnh ngắn hộp bao < nMin ô hoặc diện tích < nMin²/2 ô² (gần trùng, gần thẳng hàng), có hysteresis khi đang mở; không snap, không kẹp (điểm trong bảng nên tứ giác không tràn bảng, `limited` chỉ còn cho chuột). `buildMask(shape, layout, mirror, epoch, { limited, prev, hysteresisCells })` là nơi duy nhất rasterize (I2), `prev` là mask frame trước cùng phiên mở. Compositor clip bằng path hợp các ô mở (hộp đầy thì một rect) rồi một `drawImage` hộp bao; viền là cạnh biên của hợp ô lùi 1 px, tứ giác nét đứt; mặt clip cùng path. `restrictedFrame` crop hộp bao rồi tô đệm xám các `holesCam` trước letterbox (I1: buffer chỉ còn pixel ô mở). `validateFace`: full khi bbox (stage) nằm trọn trong hợp ô mở và trong ROI lùi 4 %, partial khi chạm ô mở, bỏ khi chỉ nằm trong lỗ; landmark trong lỗ bị lọc. `FrameOutput.reveal` mang `shape`, `box`, `cells`. Vòng lặp đóng `too-small` nếu tập ô rỗng. Hệ quả hysteresis: ô ở mép còn lấn dưới 0,25 ô giữ trạng thái cũ (dời tay thì cột cũ ở mép rời sau cột mới vào), e2e ghi rõ | Yêu cầu của người dùng 18/09: vùng mở của bốn ngón tay không nhất thiết vuông, là hình bốn cạnh bất kỳ; ô trắng bị cạnh cắt qua cũng phải hiện. Tập ô giữ được I1 đến I4 (mask vẫn quyết định cả hiển thị và buffer, không biến dạng ảnh) với hình học thuần kiểm được bằng unit | ROI-01, ROI-02, MASK-01, MASK-02, FACE-02, INT-01, QA-01 |
| 2026-09-18 | D-039 | QA-01: gate cứng đo bằng hai cách độc lập ở mọi ca dùng nguồn tổng hợp: màu đánh dấu (`restricted.spec`) và ảnh tham chiếu dựng lại từ định nghĩa cảnh (`installGateAudit` trong `tests/e2e/helpers.ts`: crop 1:1 theo `roiCam`, lỗ xám, letterbox cùng công thức, so từng pixel, lệch quá 2 mức là lỗi, cảnh động và ảnh chưa nạp bỏ qua); e2e ghi số đo vào annotations (`note`, `expectGateClean`); Playwright thêm JSON reporter `reports/e2e.json`, Vitest `--reporter=json --outputFile=reports/unit.json` (không dùng `test-results/` vì Playwright xóa thư mục đó đầu mỗi lần chạy); `tools/test-report.mjs` sinh mục "Kết quả lần chạy" (môi trường, CPU, Node, Chromium theo `browsers.json` của Playwright, model theo `models.json`, bảng ca và số đo) giữa hai mốc trong `docs/test-report-mask.md`, phần còn lại viết tay; `LoopSnapshot.faceGate.lastAccepted` (tác vụ, epoch, ROI, tuổi, số mặt) và kịch bản `faceMaxAge(ms)` (`probes.faceMaxAgeMs`, chỉ `?debug=1`, nới tuổi tối đa của kết quả mặt) để e2e kiểm ánh xạ theo ROI cũ khi dời cửa sổ một cách tất định dù suy luận chậm dưới tải; CI GitHub Actions `.github/workflows/ci.yml` dựng ngay ở QA-01 (Node 24, cache model theo sha256 manifest, lint, boundaries, invariants, Prettier, Mermaid, unit, e2e với 2 worker và thử lại một lần khi `CI`, sinh báo cáo, build; artefact `reports/`, `test-results/`, báo cáo, `dist/`), REL-01 chỉ còn bước triển khai; `tools/spikes` bỏ khỏi Prettier vì mã spike giữ nguyên như lúc đo | QA-01 đòi mọi ca đo bằng số, chạy trong CI và có báo cáo tái tạo được; hai cách đo gate cứng bắt được cả sai lệch crop và lỗ không tô ở mọi cảnh tĩnh, không chỉ cảnh màu đánh dấu | QA-01, REL-01 |
| 2026-09-18 | D-040 | PERF-01: `core/latency.ts` (cửa sổ trượt p50/p95 ghi vòng, hạng sorted[floor(p · (n − 1))] nên p50 giữ quy ước FaceClient) dùng cho inferMs của FaceClient và HandClient (thêm `p95InferMs`) và thời gian vẽ, tick của vòng lặp (`LoopSnapshot.timing`, cửa sổ 120 frame); `debug/stats.ts` sampler 250 ms tính fps output, Hz mặt, tay, phân loại trên cửa sổ 2 s (Hz = Δđếm / Δt), rớt, chờ, epoch, trạng thái; overlay `stats-stat` qua `useSyncExternalStore(stats.subscribe)` và `window.__wct.stats` (`debug/statsProbe.ts`, tách để stats.ts không kéo khai báo global vào unit test); rate control giữ `max(1000 / targetHz, p50)` (FACE-01); soak `tests/soak/soak.spec.ts` với `playwright.soak.config.ts` riêng (`npm run test:soak`, `SOAK_MINUTES` mặc định 15, `SOAK_SAMPLE_S` 30, một worker), heap sau GC, node và listener qua CDP `Performance.getMetrics` sau `HeapProfiler.collectGarbage` (không dùng `measureUserAgentSpecificMemory` vì cần COOP/COEP), tay thật trên `hands.jpg` khi có, không thì `FakeHandsSpec.orbit` cho tay giả lập chạy quỹ đạo; `tools/benchmark-report.mjs` sinh mục kết quả của `docs/benchmark.md` với đạt/không đạt theo mục tiêu mục 3; giữ Canvas 2D (bước 6) khi thời gian vẽ p95 dưới 16,7 ms; số đo headless (SwiftShader, CPU delegate tay) là cận dưới, trình duyệt thật đo ở QA-02 | PERF-01 đòi số đo tái tạo được và soak tự động; sampler tách khỏi vòng lặp để không cấp phát theo frame và React chỉ re-render 4 Hz | PERF-01, QA-02, CLS-02 |
| 2026-09-18 | D-041 | UX-01: `app/guidance.ts` thuần: `buildGuidance` trả bước (camera → cửa sổ → khuôn mặt), tone, tiêu đề, một câu hướng dẫn và lý do đóng cho từng pha camera (`cameraPhase`, phân biệt đổi camera với xin quyền lần đầu bằng lịch sử active giữ ngoài React), từng `CloseReason` theo nguồn cửa sổ và từng `FrameOutput.status`; ưu tiên camera > tab ẩn > worker đang nạp hay lỗi > lý do đóng > trạng thái vùng mở; lớp `Guide` nổi trên canvas với `pointer-events: none` và `aria-live`; `statusMessage` (FACE-02) giữ làm dòng debug. Toàn màn hình bằng Fullscreen API trên phần tử gốc `.stage` (nút và phím F): thanh trên và hai panel thành lớp phủ absolute tự ẩn sau 2,5 s không tương tác, canvas không đổi kích thước nên epoch không tăng khi lớp phủ hiện hay ẩn. Panel cài đặt (lưới, mirror, nguồn, đảo trái/phải, slot, độ nhạy) và panel debug tách riêng, thu gọn bằng `hidden` (dòng debug vẫn trong DOM cho e2e cũ), trạng thái lưu sessionStorage `wct.ui` theo tab, panel debug mặc định mở chỉ khi `?debug=1`. Bấm lên canvas bỏ focus khỏi ô nhập để Esc, Space, F chạy | Tiêu chí UX-01 là người mới mở được cửa sổ chỉ theo hướng dẫn trên màn hình; lớp phủ không được đổi cỡ canvas vì đổi cỡ là đổi cấu hình (đóng vùng); thông điệp thuần để unit test liệt kê đủ mọi lý do; tab scope để kiosk không mang trạng thái panel sang tab khác | UX-01, UX-02, LOG-02 |
| 2026-09-18 | D-042 | UX-02: màn hình bắt đầu một màn (lưới hai cột căn giữa trong 100dvh, một cột dưới 900 px) với minh họa SVG inline (`LandingIllustration.tsx`: bảng chia ô, bốn đầu ngón màu slot, cửa sổ viền xanh, khuôn mặt cách điệu, ngoài cửa sổ trắng; không ảnh camera, không ảnh ngoài); kiểu ở `landing.css`, biến màu, chữ, khoảng cách khai báo `:root` trong `app.css` và sân khấu dùng chung; accent đổi #1a73e8 → #1967d2 trong CSS để liên kết đạt 4,5:1 trên nền surface (viền cửa sổ trên canvas giữ nguyên màu compositor); chuyển động duy nhất (nét đứt tứ giác) tắt theo `prefers-reduced-motion`; `consentScopeNote` chỉ hiện dòng kiosk khi scope là tab; cấu trúc form và cổng đồng ý của WEB-00 giữ nguyên; Lighthouse chạy tay bằng `npx lighthouse` (không thêm phụ thuộc, không trong CI), ảnh chụp bằng `npm run screenshots` vào `docs/screenshots/` | D-023 đòi màn hình bắt đầu thẩm mỹ mà không đổi hành vi WEB-00; I9 cấm tài nguyên ngoài nên minh họa phải inline; tương phản đo được trong e2e để không phụ thuộc công cụ ngoài | UX-02, WEB-00, UX-01, REL-01 |
| 2026-09-18 | D-043 | CLS-01: dataset mode lấy ảnh duy nhất qua `CropTap` của `restrictedFrame` (crop 1:1 đã tô đệm lỗ, trước letterbox; kiểu `CropMeta`, `CropTap` ở `core/types.ts`), nên mẫu luôn là đúng ảnh model nhận và không có đường nào tới frame gốc (I8); `src/dataset/**` chịu lint biên như face/ và classify/. Recorder thuần (`dataset/recorder.ts`) với mã hóa, đồng hồ, ngẫu nhiên, sink tiêm vào; nhịp theo ts frame, một mẫu đang mã hóa tại một thời điểm; metadata gồm `sizeClass` theo cạnh ngắn px (160, 320) và `position`/`edges` theo mép camera. Lưu cục bộ: thư mục qua File System Access API (Chromium) ghi xuyên qua, không thì bộ nhớ và zip stored thuần (`dataset/zip.ts`, không thêm phụ thuộc, PNG đã nén). Bắt buộc tích "người tham gia đã ký đồng ý" trước khi thu và ghi vào `session.json`; `subjectId` ẩn danh. Công cụ Python chỉ thư viện chuẩn (không PIL: đọc IHDR để kiểm cỡ), nhãn cuối `labelFinal` tách khỏi nhãn tạm, chia tập theo `subjectId` và kiểm rò rỉ cả session lẫn subject, thống kê ghi giữa hai mốc của `docs/dataset.md`; unittest Python chạy trong CI | Tiêu chí CLS-01: không frame gốc nào trong dataset, có văn bản đồng ý, có kiểm rò rỉ, có thống kê; máy phát triển và CI không có PIL hay numpy | CLS-01, CLS-02, I8, I9 |
| 2026-09-18 | D-044 | CLS-02: làm đường ống phân loại trước khi có dataset với model ONNX stub mã hóa tay (`tools/make-stub-classifier.mjs`, 360 byte, GlobalAveragePool → Flatten → Gemm: person = G − (R + B) / 2 trên màu trung bình đã chuẩn hóa) sinh trong `models:fetch`, không commit; `DEFAULTS.classifier.modelPath` trỏ tới stub cho tới khi export model thật (`/models/classifier.onnx`, sha256 trong `models.json`). Worker khởi tạo lười khi vùng mở lần đầu (không tốn ORT cho trang không mở vùng; kết quả đầu trễ vài giây, chấp nhận với 3 đến 5 Hz) và nạp bundle `onnxruntime-web/webgpu` chỉ khi `navigator.gpu.requestAdapter()` trả adapter (headless shell có `navigator.gpu` nhưng không có adapter), không thì `onnxruntime-web`; wasm đơn luồng (không COOP/COEP); `models:fetch` copy 4 file loader vào `public/models/ort/`; worker vẽ bitmap letterbox về cạnh input trên canvas riêng (drawImage 9 tham số, `check-invariants` cho phép file này vì worker không có đường nào tới video). Classifier nhận bitmap thứ hai của cùng crop từ builder (`copies`), một tác vụ, nhịp `max(250, p50)`, chỉ khi cạnh ROI ≥ 96 px; kết quả qua gate epoch, taskId đã loại, tuổi ≤ 600 ms, vùng còn mở; nhãn giữ ≤ 1,5 s và gắn vào từng mặt bằng quy tắc unknown thuần (`subjectRule.ts`, thêm `ValidatedFace.visible`); chuẩn hóa `(x / 255 − 0,45) / 0,225` dùng chung với script huấn luyện. Script huấn luyện, export, kiểm export và đánh giá (`tools/train/`) viết theo torchvision và onnxruntime Python, chưa chạy trên máy phát triển; metric thuần có unittest | Chưa có dataset thật nên không thể có model; đường ống, gate và UI cần được kiểm tự động ngay với nhãn xác định; tránh phụ thuộc gói onnx Python để tạo stub | CLS-02, QA-02, D-013, I1 |
| 2026-09-19 | D-045 | QA-02: benchmark nhiều trình duyệt `tests/bench/bench.spec.ts` với `playwright.bench.config.ts` (`npm run test:bench`): project cho Chromium headless shell, Chrome và Edge cài trên máy qua `channel` (GPU thật kể cả headless), Firefox và WebKit khi đã cài cho Playwright; bốn ca (môi trường qua `window.__wct.env` và dòng `env-stat` của `debug/envProbe.ts` + `envText.ts`, cửa sổ chuột với khoảng cách và tuổi kết quả đo theo rAF trong trang, phân loại ép wasm, tay CPU rồi GPU trên `hands.jpg`); tham số URL `hands=GPU\|CPU` và `ep=wasm`; `tools/benchmark-report.mjs` gộp `reports/bench-*.json` vào `docs/benchmark-matrix.json` và sinh ma trận cùng bảng đối chiếu tham số trong `docs/benchmark.md`. Tham số chốt theo số đo (benchmark.md mục 6): `hands.delegate` = `auto` (`hands/handDelegate.ts`: GPU khi WebGL trên phần cứng, CPU khi renderer là phần mềm hay thiếu WebGL; thay D-009); `freshness.pointMaxAgeMs` 150 ms với GPU delegate và `pointMaxAgeMsCpu` 250 ms với CPU (độ nhạy mặc định theo delegate lúc mở app và khi đặt lại); giữ `faceResultMaxAgeMs` 250, `classifier.resultMaxAgeMs` 600, `labelMaxAgeMs` 1500, `hysteresisCells` 0,25, EP `['webgpu', 'wasm']` (D-013), `face.delegate` GPU. Firefox và Safari chưa đo (không có binary; bảng API và fallback viết theo MDN, cách kiểm thủ công ghi trong benchmark.md); metric phân loại mục 7.3 chờ model thật | Trên RTX 3050 (Chrome, Edge 153) tay GPU delegate 28,5 đến 29,5 Hz so với CPU 10,5 đến 11 Hz, trên SwiftShader GPU 2 Hz so với CPU 9,5 Hz; tuổi điểm cần khoảng cách p95 + inferMs p95 ≈ 80 đến 100 ms (GPU) và 190 đến 210 ms (CPU); tuổi kết quả mặt lúc nhận p95 68 ms (GPU thật), 145 ms (headless), quá tuổi 0; stub: wasm 4,7 ms nhanh hơn webgpu 19,6 ms nhưng S6 với MobileNetV2 ngược lại nên giữ thứ tự EP tới khi có model thật | QA-02, D-009, D-013, D-040, REL-01 |
| 2026-09-19 | D-046 | LOG-02 (tùy chọn theo D-022): nhật ký vận hành cục bộ trong `src/log/` (`localLog.ts` thuần: bảy loại sự kiện metadata `consent`, `camera-start`, `camera-stop`, `camera-error`, `reveal-open`, `reveal-close`, `config-change`; payload ≤ 1 KB JSON, quá thì `{ truncated, keys }`; tối đa 10 000 bản ghi hoặc 30 ngày, xóa cũ trước, dọn lúc start và mỗi 50 lần ghi; công tắc mặc định tắt lưu localStorage `wct.log`; `logOnce` cho consent của phiên; `idbStore.ts` IndexedDB `wct-log`, dự phòng kho bộ nhớ) và `lint:boundaries` cấm `src/log/**` import ngoài `core/**`; StagePage xây payload từ snapshot chữ và số (camera, store, vòng lặp); thanh `LogControls` trong panel cài đặt (công tắc, bảng lọc loại và ngày, Xuất CSV qua `Blob` + `<a download>`, Xóa) và trạng thái ở thanh trên; `window.__wct.log` với `appendRaw` để e2e kiểm giới hạn | D-022 chốt nhật ký chỉ cục bộ, không backend (I9); người vận hành máy trình diễn cần xem lại phiên (khi nào camera bật, vùng mở vì nguồn nào, đóng vì lý do gì) mà không lộ ảnh: payload chỉ là metadata và lint chặn đường tới frame; IndexedDB giữ qua tải lại, giới hạn tránh phình | LOG-02, D-022, I9, UX-01 |
| 2026-09-19 | D-047 | ROI-03 (thay D-034 và phần hình của D-038): vùng mở theo tay là **bao lồi** của mọi đầu ngón đã chọn (mặc định cả năm: cái, trỏ, giữa, áp út, út) của mọi bàn tay đang theo dõi, không còn bốn slot gắn cứng tay và ngón. `hands/fingertips.ts` đánh giá từng track × ngón (uncertain, ngoài bảng, tuổi điểm theo độ nhạy, score); điểm không hợp lệ chỉ bị loại khỏi bao lồi; vùng mở khi còn ≥ `reveal.minPoints` (3) điểm hợp lệ của ≥ `hands.minHands` (2) tay, đóng với `few-points` (thay `missing-slot`) hoặc lý do trội của điểm không hợp lệ (`out-of-board` > `stale-point`; `low-score` tính như thiếu), `ambiguous-hands` ưu tiên cao nhất. `reveal/hullSolver.ts` (thay `quadSolver.ts`): One Euro theo khóa `${trackId}:${tip}`, `convexHull` (core/cells.ts), too-small như D-038. `RevealShape` `polygon`; `FrameOutput.points` một phần tử mỗi đầu ngón (hand, tip, trackId, valid, reason, pStage); store `fingers` (UI `FingerControls` chọn ngón, đổi thì epoch++); chấm đầu ngón màu theo tay; hướng dẫn theo số điểm và tay còn thiếu; kịch bản `fingers(...)` cho e2e | Yêu cầu theo dõi toàn bộ bàn tay và tính vùng từ mọi đầu ngón thay vì hai ngón mỗi tay; bao lồi cho đa giác đơn, lồi với số điểm bất kỳ (đa giác nối theo góc quanh tâm tự cắt và răng cưa với mười điểm); giữ hai tay để cử chỉ đóng khung và tính chất "bỏ một tay thì đóng" của INT-01; loại điểm thay vì đóng để ngón bị che hay ra mép chỉ làm vùng co lại | ROI-03, D-034, D-038, INT-01, UX-01 |
| 2026-09-19 | D-048 | REL-01 (thay phần triển khai của D-024): bản công khai triển khai lên GitHub Pages bằng job `deploy` trong `ci.yml` (`needs: check`, chỉ khi push `main`, `configure-pages@v5` → `upload-pages-artifact@v4` → `deploy-pages@v4`, Pages Source = GitHub Actions, không cần `.nojekyll` hay file `CNAME`); gốc đường dẫn qua biến build `VITE_BASE` và hàm thuần `withBase()` trong `core/config.ts` (địa chỉ tạm `https://<user>.github.io/<repo>/` cần base `/<repo>/`, biến repo `PAGES_BASE`); `public/spike-assets/` giữ cho dev và e2e cục bộ nhưng bị xóa khỏi `dist/` ở `closeBundle`; service worker cache model và wasm trở thành bắt buộc (`public/sw.js` thuần, cache-first `models/*` và `assets/*` theo khóa sha256 của `models.json`, `index.html` network-first, chỉ cùng origin, không precache lúc install); tên miền riêng mua sau: A/AAAA tới IP GitHub Pages, `www` CNAME `<user>.github.io`, Enforce HTTPS, rồi `PAGES_BASE` về `/`; kiểm sau triển khai, số đo tải và cách rollback ghi `docs/deploy.md` | Mọi người mở được ứng dụng bằng một địa chỉ thay vì clone repo; app thuần tĩnh nên không cần máy chủ; GitHub Pages dùng ngay CI đã có (D-039), không giới hạn 25 MiB mỗi file như Cloudflare Pages (wasm ORT 27 MB) và gắn tên miền miễn phí; hạn 1 GB site và 100 GB/tháng đủ cho pilot (tải lần đầu 35 đến 50 MB, service worker đưa lần sau về 0); đường dẫn tuyệt đối `/models/...` hiện tại không chạy được dưới `/<repo>/` | REL-01, D-024, D-020, D-013, D-039, mục 4 (Triển khai), 7.27, 9.3 |
| 2026-09-19 | D-049 | UX-03 (mở rộng UX-01, UX-02 và D-041, D-042): (1) bộ token điều khiển trong `app.css`: cỡ control 32 px, bậc nút (mặc định là nút phụ viền, `.primary` chỉ cho hành động chính của mỗi khối: Bật camera, Bắt đầu, Bắt đầu thu; `.danger` cho xóa; `.link` cho lối thoát; không còn nút đen đặc), select và ô nhập cùng viền, checkbox vẽ thành công tắc (vẫn là `input[type=checkbox]` trong `label.check`), chip đầu ngón (`label.chip`), pill trạng thái camera có chấm màu theo pha (vẫn `role=status` với câu đầy đủ); (2) thanh trên ba vùng một dòng ở 1280 px (brand là liên kết về trang chào, camera | pill trạng thái co giãn, cắt bớt bằng dấu ba chấm | Cài đặt, Debug, Trình diễn, Toàn màn hình, Thu hồi đồng ý), bỏ liên kết "Về trang chào" riêng; (3) panel cài đặt là `aside` 320 px bên phải chia mục dọc (Lưới, Cửa sổ, Đầu ngón và Độ nhạy chỉ khi nguồn tay, Thu dữ liệu, Nhật ký cục bộ), độ nhạy là thanh trượt kèm ô số (thanh trượt `aria-hidden`, ô số giữ aria-label cho e2e), nguồn cửa sổ vẫn là `<select>`; panel debug là ngăn kéo dưới canvas (lưới ba cột mono, tối đa 150 px, cuộn), mọi `data-testid` giữ nguyên nội dung; cả hai vẫn thu gọn bằng `hidden` và nằm trong luồng (thu gọn cột trả lại chiều rộng, mở ngăn kéo lấy chiều cao: epoch++ theo D-041); (4) chế độ trình diễn `ui.present` (nút Trình diễn `aria-pressed`, mặc định theo `?mode=present`, lưu `wct.ui`, cột cài đặt đóng sẵn khi vào bằng URL) dùng chung lớp `.overlay` với toàn màn hình: `.chrome`, `aside` và ngăn kéo đều absolute (pill nổi giữa trên, tấm kính bên phải, HUD tối trên trái), canvas chiếm trọn `.stage` nên mở panel không đổi cỡ canvas và không đóng cửa sổ; `useIdle` (tách từ `useFullscreen`) ẩn lớp nổi sau 2,5 s không tương tác, còn một vạch gợi ý; lớp hướng dẫn bản `.hud` chữ lớn; (5) màn hình bắt đầu: eyebrow, stepper, hộp đồng ý viền accent khi đã tích, minh họa SVG tĩnh thay bằng canvas động `LandingPreview` (hình học thuần `landingScene.ts` dùng `convexHull`, `rasterizePolygon`, quy tắc mặt full/partial của app; không camera, không ảnh, không drawImage; một khung tĩnh khi `prefers-reduced-motion`); `?mode=present` là màn hình bắt đầu kiosk (lưới phủ cả màn với cửa sổ mẫu trôi, thẻ đồng ý nổi, nút 56 px) rồi vào `#/app?mode=present`. Mặc định của app (bố cục trong luồng) giữ nguyên nên e2e cũ chỉ đổi một khẳng định (thu gọn cột: rộng hơn thay vì cao hơn) và selector minh họa (canvas thay svg) | Đo ngày 2026-09-19 ở 1280 × 720: mở hết panel thì canvas còn 325 px (45 %, ô 9 px), 39 control trên 7 thanh không nhãn nhóm, thanh trên 81 px hai dòng, bốn nút đen cùng kiểu cho hành động chính, phụ và xóa; cột bên trả canvas về 960 × 522 (ô 14 px) và trình diễn về 1280 × 720 (ô 20 px); lớp nổi không đổi cỡ canvas là đúng lý do của D-041; kiosk cần một địa chỉ bookmark được | UX-03, UX-01, UX-02, D-041, D-042, REL-01 |
| 2026-09-20 | D-050 | REL-01 phần triển khai tại chỗ (bổ sung D-048): (1) `models.json` `ort.files` là cặp `asyncify` (bundle `onnxruntime-web/webgpu`) và cặp `jsep` (bundle `onnxruntime-web`), `wasm.files` chỉ `vision_wasm_module_internal.{js,wasm}`; `models:fetch` copy theo danh sách và xóa file thừa; unit đối chiếu tên loader trong bundle của `node_modules` với manifest; `vite.config.ts` `resolve.conditions` `onnxruntime-web-use-extern-wasm` để không bundle wasm ORT vào `dist/assets`. (2) `VITE_BASE` qua `loadEnv` và `resolveBase()` (dạng `/` hoặc `/ten/`); `withBase()` và `modelUrls()` trong `core/config.ts`, áp ở ba nơi tiêu thụ, `DEFAULTS` giữ `/models/...`. (3) Plugin `wctBuild` ở `closeBundle`: xóa `dist/spike-assets`, thay khóa trong `dist/sw.js`, in dung lượng. (4) Service worker `public/sw.js` thuần với hai cache `wct-models-<sha256 models.json>` và `wct-app-<sha256 danh sách dist/assets>`, khóa thay lúc build; `models/*`, `assets/*` cache-first, trang network-first; khóa cache là URL bỏ hash tra với `ignoreVary`; chỉ 200; `activate` xóa `wct-*` khác và `clients.claim`; không precache, `StagePage` gửi `warm` (asset trang từ Resource Timing + `modelWarmList()`); đăng ký `src/app/registerSw.ts` chỉ PROD sau `load`. (5) I9 ở runtime: `core/networkGuard.ts` thay `fetch` của ba worker và trang bằng bản chỉ cùng origin vì `@mediapipe/tasks-vision` 1.0.1 POST thống kê tới `odml.pa.googleapis.com` mỗi 60 s không tắt được; `check:invariants` đòi mọi worker và `main.tsx` cài guard; service worker trả `Response.error()` cho yêu cầu khác origin. (6) `tests/deploy/sw.spec.ts` + `playwright.deploy.config.ts` (`npm run test:deploy`) chạy trên `dist/` qua `vite preview`, project `chromium` và `chrome` khi có, đo ở tầng context (`context.setOffline`, `context.on('request')`) vì Playwright quản lý yêu cầu của service worker ở đó; kết quả `reports/deploy.json` vào `docs/test-report-mask.md` mục riêng. (7) CI: `check` build với `VITE_BASE` từ `vars.PAGES_BASE` (mặc định `/<repo>/`), kiểm build sạch, `test:deploy`, `upload-pages-artifact` khi push `main`; job `deploy` `configure-pages` → `deploy-pages`; concurrency chỉ hủy với PR. `.claude/` vào `.gitignore` | Rà soát D-048 với mã thật: bản build hỏng phân loại trên máy có WebGPU (thiếu loader asyncify), ca service worker theo D-048 dùng `page.route`/`page.on` sẽ xanh giả, một khóa cache cho cả model và asset làm cache phình, và MediaPipe gửi telemetry là vi phạm I9 mà lint không thấy; đo được: `dist/` 253 → 80 MB; lần mở đầu cache 7 + 7 file, lần hai 12/12 phản hồi từ service worker, lần ba offline vẫn chạy, 0 yêu cầu khác origin trên cả Chromium và Chrome (WebGPU) với base `/` và `/repo/` | REL-01, D-048, D-024, D-013, D-019, mục 7.27, 9.3 |
