# Fake camera fixture (TEST-00)

`camera.y4m` is generated by `tools/make_test_clips.py` and is not committed (`*.y4m` in .gitignore). When the file exists, `playwright.config.ts` automatically adds `--use-file-for-fake-video-capture`; when it does not, Chromium uses its default fake source.
